<?xml version="1.0" encoding="utf-8"?>

<service-descriptor id="common/batch">

	<service id="send.batch.email" label="Quartz를 통한 E-mail 발송">
		<script>
			<![CDATA[
				var param = new Map();
				var mailList     = $jdbc.queryList("admin/template", "batch.email.list", param);

				$data.idx = 0;
				$data.mails		    = new List();

				var snd_title    = "";
				var snd_subject  = "";
				var mail = new Map();
				try {
	            $logger.info("메일 발송 건수 >>  :: " + mailList.size());
					for(var i = 0; i < mailList.size(); i++ ) {
					    var mailInfo = mailList.get(i);
					    snd_title    = mailInfo.snd_title;
					    snd_subject  = mailInfo.snd_subject;

						///////////////////////////////////////////////////////////////
						// 1. EMAIL내용 준비
						///////////////////////////////////////////////////////////////
						mail = new Map();
						mail.from			= mailInfo.from_email;
						mail.fromName		= mailInfo.from_name;
						mail.to				= mailInfo.to_email;
						mail.toName			= mailInfo.to_name;

						mail.snd_title		= snd_title;           //메일 제목
						mail.snd_subject    = snd_subject;         //메일 본문
						mail.url			= mailInfo.url;        //공통메일 템플릿
		                mail.image_url      = mailInfo.image_url;  //메일  JSP 참조하기 위한 URL(WAS)
		                mail.comp_cd        = mailInfo.comp_cd;    //각사별 메일 템플릿 구분자
	                    mail.email_seq      = mailInfo.email_seq;
						if(mailInfo.to_email != '' && mailInfo.to_email != null) {
							$data.mails.add(mail);
						}
					}
				}
				catch(e) {
					$logger.info(e.toString());
				}finally{
					importPackage(Packages.volcano.custom.interfacemodule);
					var batchParam = new Map();

					// 현재는 배치를 통해서 원내메일이 발송되지 않고 있으며
					// 업무단에서 필요한 경우 직접적으로 바로 인터페이스를 콜하고있다.
					// 차후 배치를 통해 내보내야 하는경우 insert.gwemail 쿼리를 이용해서 내용을 집어 넣으면 된다
					var gwMailList     = $jdbc.queryList("admin/template", "batch.gwemail.list", param);
					for(var i = 0; i < gwMailList.size(); i++ ){
						batchParam = new Map();
						batchParam.if_cd = 'IF1140';
						batchParam.email_seq = gwMailList[i].msgid;
						var inter = new InterfaceSendProvider();
						inter.sendInterface(batchParam);
					}
				}
	            $logger.info(">>  :: " + $data.idx + ", mail size : " + $data.mails.size());

			]]>
		</script>
		<while expression="{$data.idx &lt; $data.mails.size()}">
			<script>
				<![CDATA[
				$data.mail = $data.mails[$data.idx];
				$data.idx = $data.idx+1;

				$jdbc.update("admin/template", "batch.update.email", $data.mail);
				$jdbc.commit();
				$logger.info("index ====> " + $data.idx);
				]]>
			</script>
			<mail subject="{$data.mail.snd_title}" content-url="{$data.mail.url}">
				<sender email="{$data.mail.from}" name="{$data.mail.fromName}" />
				<receiver email="{$data.mail.to}" name="{$data.mail.toName}" type="TO"/>
				<parameters parameter-name="mail">
					<parameter name="snd_title"/>
					<parameter name="snd_subject"/>
					<parameter name="image_url"/>
					<parameter name="comp_cd"/>
				</parameters>
			</mail>
		</while>
	</service>

	<service id="send.po.info" label="I/F발주데이터 거래처전송">
		<script>
			<![CDATA[

				var batchParam = new Map();
				batchParam.grp_cd = 'S999';

				var batchAdmin = new Map();
				batchAdmin = $jdbc.query("admin/template", "batch.exception.admin", batchParam);
				if (batchAdmin.cddl_cnt == 0){
					var exmld         	= $xmlManager.getDescriptor("properties");
					var sms_send_no    	= exmld.getDescriptorElement("sms.send.no").getText();
					var serverPath  	= exmld.getDescriptorElement("server.url").getText();    //메일  JSP 참조하기 위한 URL(WAS)
					var webserverPath   = exmld.getDescriptorElement("webserver.url").getText(); //메일 image URL(외부웹서버)
					var email_send_nm   = exmld.getDescriptorElement("email.send.nm").getText(); //메일 수신자명(세션값 없는경우)
					var email_send_ad   = exmld.getDescriptorElement("email.send.ad").getText(); //메일 수신자 메일주소(세션값 없는경우)

					var param = new Map();

					//발주에 VD_CD가 없는 데이터를 ESMVDGL에 생성되어 있으면 업데이트한다.
					//$jdbc.update("procurement/po", "update.po.vdcd", param);

					//구매요청 물품에 VD_CD가 없는 데이터를 ESMVDGL에 생성되어 있으면 업데이트한다.
					//$jdbc.update("pr/prm", "update.pr.vdcd", param);

					//메일 및 SMS보내는 대상 업체담당자 조회한다.
					var list = $jdbc.queryList("procurement/po", "select.po.list.send", param);

					var udpClient = new UDPClient();

					for(var i=0; i < list.size(); i++){

						/*
						/////////////////////////////////////////////////////////////////////////////
						//SMS메세지 보내기
						/////////////////////////////////////////////////////////////////////////////
						*/
						var smsMap = new Map();

						/*
						    1.예약일자
							YYYYMMDDHHMMSS(년월일시분초)로 입력
							8자리 이하(YYYYMMDD)면 즉시 발송으로 처리
							9자리 이상(YYYYMMDDH)면 이후 값을 '0'으로 채원 예약처리
							숫자가 아닌 문자열이 있으면 오류처리.
						*/
						smsMap.send_dt = "";

						/*
						    2.발신자번호
							숫자와 '-'만 가능
						*/
						if (list.get(i).full_phone_no == null){
							// Default 송신자번호
							smsMap.send_no = sms_send_no;
						}
						else {
							smsMap.send_no = list.get(i).full_phone_no;
						}

						/*
						    3.수신자번호e
							숫자와 '-'만 가능
						*/
						smsMap.receive_no = list.get(i).chr_mobile;
						//smsMap.receive_no = '';

						/*
						    4.메세지
							80byte 이내로 가능
						*/
						smsMap.sms_tmp_id = "S04";
						sendMessage = $jdbc.query("admin/template", "select.sms.template.info", smsMap);
						sendMessage.conts = sendMessage.conts.replace('%po_typ_nm%', list.get(i).po_typ_nm);
						sendMessage.conts = sendMessage.conts.replace('%typ_nm%', list.get(i).typ_nm);
						sendMessage.conts = sendMessage.conts.replace('%po_no%', list.get(i).po_no);
						sendMessage.conts = sendMessage.conts.replace('%comp_nm%', list.get(i).comp_nm);
						smsMap.message = sendMessage.conts;

						/*
						    5.로그인 사번
						*/
						smsMap.usr_id = list.get(i).purc_id;


						/*
						    6.병원구분코드
						*/
						smsMap.comp_cd = list.get(i).comp_cd;
						var ret_val = "00";

						//발송여부가 'Y'인 경우만 발송
						if(sendMessage.use_yn == "Y"){

							ret_val = udpClient.sendToSMSBatch(smsMap);
							$logger.info("Receive No :" + smsMap.receive_no + " // Result ====>" + ret_val);
						}

						/*
						/////////////////////////////////////////////////////////////////////////////
						//SMS메세지 보내기
						/////////////////////////////////////////////////////////////////////////////
						*/


						/*
						/////////////////////////////////////////////////////////////////////////////
						//E-mail batch에 등록하기
						/////////////////////////////////////////////////////////////////////////////
						*/
						var saveParam = $data.saveItem;
						var param = new Map();

						var comp_cd = list.get(i).comp_cd;
						var mailContents = "";

						///////////////////////////////////////////////////////////////
						// 1. 메일 템플릿 정보를 가져온다.
						///////////////////////////////////////////////////////////////
						param.comp_cd_yn	= 'N';    // 병원별 템플릿이 동일 할 경우는 'N'
						param.mail_tmp_id   = 'T03';  // I/F발주정보 거래처전송

						mailContents = $jdbc.query("admin/template", "select.mail.template.info", param);

						var snd_title = "";
						var snd_subject = "";

						if( mailContents ) {
							snd_title    = mailContents.snd_title;
							snd_subject  = mailContents.snd_subject;

							// 제목
							snd_title = snd_title.replace('%po_typ_nm%', list.get(i).po_typ_nm);
							// 내용
							snd_subject = snd_subject.replace('%po_typ_nm%', list.get(i).po_typ_nm);
							snd_subject = snd_subject.replace('%full_comp_nm%', list.get(i).full_comp_nm);
							snd_subject = snd_subject.replace('%typ_nm%', list.get(i).typ_nm);
							snd_subject = snd_subject.replace('%po_no%', list.get(i).po_no);
						}

						///////////////////////////////////////////////////////////////
						// 2. EMAIL내용 준비
						///////////////////////////////////////////////////////////////

						if( mailContents ) {
							$data.mails         = new List();

							var mail = new Map();
							mail.from           = email_send_ad;
							mail.fromName       = email_send_nm;
							mail.to             = list.get(i).chr_email;
							mail.toName         = list.get(i).chr_nm;

							mail.snd_title      = snd_title;   //메일 제목
							mail.snd_subject    = snd_subject; //메일 본문
							mail.url            = serverPath + "/mail/templateMail.jsp"; //공통메일 템플릿
							mail.image_url      = webserverPath + "/mail/img/";  //메일  JSP 참조하기 위한 URL(WAS)
							mail.comp_cd        = list.get(i).comp_cd;  //병원별 메일 템플릿 구분자

							$data.mails.add(mail);

							$logger.info('size----------------' + $data.mails.size());

							//발송여부가 'Y'인 경우만 이메일 발송
							if(mailContents.use_yn == "Y"){
								for(var y = 0 ; y < $data.mails.size(); y++){
									$data.mail = $data.mails[y];
									$jdbc.update("admin/template", "insert.email", $data.mail);
								}
							}
						}

						/*
						/////////////////////////////////////////////////////////////////////////////
						//E-mail batch에 등록하기
						/////////////////////////////////////////////////////////////////////////////
						*/

						// 발주 모바일 처리 ********************************************************
	                    var mobile_map = list.get(i);
	                    if(mobile_map.typ == "PO"){
	                    	mobile_map.tmp_id = "T01";
	                    }else{
	                    	mobile_map.tmp_id = "T05";
	                    }

	                    //템플릿 조회
	                    var tmp_info = $jdbc.query("unico", "select.unico.tmp.ifno", mobile_map);

	                    //수신자 조회
	                    var mobile_list = $jdbc.queryList("unico", "select.unico.send.ifno.batch", mobile_map);

	                    for(var z=0; z < mobile_list.size(); z++){
		                    var mobile = mobile_list[z];
							mobile.sdr_id = "APROS";						//송신자아이디
							mobile.sdr_nm = "아산재단";						//송신자명
							mobile.rvr_id = mobile_map.vd_cd;				//수신자아이디
							mobile.rvr_nm = mobile_map.vd_nm_loc;			//수신자 명
							mobile.rvr_cmp_nm = mobile_map.vd_nm_loc; 		//수신자 회사명
							mobile.rvr_phn_num = mobile_map.chr_mobile;		//수신자 전화번호
		                    if(mobile_map.typ == "PO"){
								mobile.snd_msg = tmp_info.snd_msg.replace('$po_no$', mobile_map.po_no);
								mobile.snd_msg = mobile.snd_msg.replace('$po_tit$', mobile_map.po_tit);
								mobile.clb_url = tmp_info.clb_url.replace('$po_no$', mobile_map.po_no);
		                    }else{
								mobile.snd_msg = tmp_info.snd_msg.replace('$rfq_no$', mobile_map.rfq_no);
								mobile.snd_msg = mobile.snd_msg.replace('$rfq_tit$', mobile_map.rfq_tit);
								mobile.clb_url = tmp_info.clb_url.replace('$rfq_no$', mobile_map.rfq_no);
		                    }

							$jdbc.update("unico", "insert.unico", mobile);
	                    }
						// ******************************************************************************

						//발주테이블 저장
						if(list.get(i).typ == "PO"){
							$jdbc.update("procurement/po", "update.po.send", list.get(i));
						}else{
							$jdbc.update("procurement/po", "update.pn.send", list.get(i));
						}

						smsMap.ret_val = "";
					}
				}

			]]>
		</script>
	</service>

	<service id="send.po.info.manual" label="발주데이터 거래처수동전송">
		<script>
			<![CDATA[
				importClass(Packages.emro.util.StringUtil);
				var strUtil     = new StringUtil();
				var result   = new Map();
				var param = $data.param;

				try{
					var exmld         	= $xmlManager.getDescriptor("properties");
					var sms_send_no    	= exmld.getDescriptorElement("sms.send.no").getText();
					var serverPath  	= exmld.getDescriptorElement("server.url").getText();    //메일  JSP 참조하기 위한 URL(WAS)
					var webserverPath   = exmld.getDescriptorElement("webserver.url").getText(); //메일 image URL(외부웹서버)
					var email_send_nm   = exmld.getDescriptorElement("email.send.nm").getText(); //메일 수신자명(세션값 없는경우)
					var email_send_ad   = exmld.getDescriptorElement("email.send.ad").getText(); //메일 수신자 메일주소(세션값 없는경우)

					//메일 및 SMS보내는 대상 업체담당자 조회한다.
					var list = $jdbc.queryList("procurement/po", "select.po.list.send.manual", param);

					var udpClient = new UDPClient();

					for(var i=0; i < list.size(); i++){

						/*
						/////////////////////////////////////////////////////////////////////////////
						//SMS메세지 보내기
						/////////////////////////////////////////////////////////////////////////////
						*/
						var smsMap = new Map();

						/*
						    1.예약일자
							YYYYMMDDHHMMSS(년월일시분초)로 입력
							8자리 이하(YYYYMMDD)면 즉시 발송으로 처리
							9자리 이상(YYYYMMDDH)면 이후 값을 '0'으로 채원 예약처리
							숫자가 아닌 문자열이 있으면 오류처리.
						*/
						smsMap.send_dt = "";

						/*
						    2.발신자번호
							숫자와 '-'만 가능
						*/
						if (list.get(i).full_phone_no == null){
							// Default 송신자번호
							smsMap.send_no = sms_send_no;
						}
						else {
							smsMap.send_no = list.get(i).full_phone_no;
						}

						/*
						    3.수신자번호e
							숫자와 '-'만 가능
						*/
						smsMap.receive_no = list.get(i).chr_mobile;

						/*
						    4.메세지
							80byte 이내로 가능
						*/
						smsMap.sms_tmp_id = "S04";
						sendMessage = $jdbc.query("admin/template", "select.sms.template.info", smsMap);
						sendMessage.conts = sendMessage.conts.replace('%po_typ_nm%', list.get(i).po_typ_nm);
						sendMessage.conts = sendMessage.conts.replace('%typ_nm%', list.get(i).typ_nm);
						sendMessage.conts = sendMessage.conts.replace('%po_no%', list.get(i).po_no);
						sendMessage.conts = sendMessage.conts.replace('%comp_nm%', list.get(i).comp_nm);
						smsMap.message = sendMessage.conts;

						/*
						    5.로그인 사번
						*/
						smsMap.usr_id = list.get(i).purc_id;


						/*
						    6.병원구분코드
						*/
						smsMap.comp_cd = list.get(i).comp_cd;
						var ret_val = "00";

						//발송여부가 'Y'인 경우만 발송
						if(sendMessage.use_yn == "Y"){
							ret_val = udpClient.sendToSMSBatch(smsMap);
						}

						/*
						/////////////////////////////////////////////////////////////////////////////
						//SMS메세지 보내기
						/////////////////////////////////////////////////////////////////////////////
						*/


						/*
						/////////////////////////////////////////////////////////////////////////////
						//E-mail batch에 등록하기
						/////////////////////////////////////////////////////////////////////////////
						*/
						var saveParam = $data.saveItem;
						var param = new Map();

						var comp_cd = list.get(i).comp_cd;
						var mailContents = "";

						///////////////////////////////////////////////////////////////
						// 1. 메일 템플릿 정보를 가져온다.
						///////////////////////////////////////////////////////////////
						param.comp_cd_yn	= 'N';    // 병원별 템플릿이 동일 할 경우는 'N'
						param.mail_tmp_id   = 'T03';  // I/F발주정보 거래처전송

						mailContents = $jdbc.query("admin/template", "select.mail.template.info", param);

						var snd_title = "";
						var snd_subject = "";

						if( mailContents ) {
							snd_title    = mailContents.snd_title;
							snd_subject  = mailContents.snd_subject;

							// 제목
							snd_title = snd_title.replace('%po_typ_nm%', list.get(i).po_typ_nm);
							// 내용
							snd_subject = snd_subject.replace('%po_typ_nm%', list.get(i).po_typ_nm);
							snd_subject = snd_subject.replace('%full_comp_nm%', list.get(i).full_comp_nm);
							snd_subject = snd_subject.replace('%typ_nm%', list.get(i).typ_nm);
							snd_subject = snd_subject.replace('%po_no%', list.get(i).po_no);
						}

						///////////////////////////////////////////////////////////////
						// 2. EMAIL내용 준비
						///////////////////////////////////////////////////////////////

						if( mailContents ) {
							$data.mails         = new List();

							var mail = new Map();
							mail.from           = email_send_ad;
							mail.fromName       = email_send_nm;
							mail.to             = list.get(i).chr_email;
							mail.toName         = list.get(i).chr_nm;

							mail.snd_title      = snd_title;   //메일 제목
							mail.snd_subject    = snd_subject; //메일 본문
							mail.url            = serverPath + "/mail/templateMail.jsp"; //공통메일 템플릿
							mail.image_url      = webserverPath + "/mail/img/";  //메일  JSP 참조하기 위한 URL(WAS)
							mail.comp_cd        = list.get(i).comp_cd;  //병원별 메일 템플릿 구분자

							$data.mails.add(mail);

							$logger.info('size----------------' + $data.mails.size());

							//발송여부가 'Y'인 경우만 이메일 발송
							if(mailContents.use_yn == "Y"){
								for(var y = 0 ; y < $data.mails.size(); y++){
									$data.mail = $data.mails[y];
									$jdbc.update("admin/template", "insert.email", $data.mail);
								}
							}
						}

						/*
						/////////////////////////////////////////////////////////////////////////////
						//E-mail batch에 등록하기
						/////////////////////////////////////////////////////////////////////////////
						*/

						// 발주 모바일 처리 ********************************************************
						/*
	                    var mobile_map = list.get(i);
	                    if(mobile_map.typ == "PO"){
	                    	mobile_map.tmp_id = "T01";
	                    }else{
	                    	mobile_map.tmp_id = "T05";
	                    }

	                    //템플릿 조회
	                    var tmp_info = $jdbc.query("unico", "select.unico.tmp.ifno", mobile_map);

	                    //수신자 조회
	                    var mobile_list = $jdbc.queryList("unico", "select.unico.send.ifno.batch", mobile_map);

	                    for(var z=0; z < mobile_list.size(); z++){
		                    var mobile = mobile_list[z];
							mobile.sdr_id = "APROS";						//송신자아이디
							mobile.sdr_nm = "아산재단";						//송신자명
							mobile.rvr_id = mobile_map.vd_cd;				//수신자아이디
							mobile.rvr_nm = mobile_map.vd_nm_loc;			//수신자 명
							mobile.rvr_cmp_nm = mobile_map.vd_nm_loc; 		//수신자 회사명
							mobile.rvr_phn_num = mobile_map.chr_mobile;		//수신자 전화번호
		                    if(mobile_map.typ == "PO"){
								mobile.snd_msg = tmp_info.snd_msg.replace('$po_no$', mobile_map.po_no);
								mobile.snd_msg = mobile.snd_msg.replace('$po_tit$', mobile_map.po_tit);
								mobile.clb_url = tmp_info.clb_url.replace('$po_no$', mobile_map.po_no);
		                    }else{
								mobile.snd_msg = tmp_info.snd_msg.replace('$rfq_no$', mobile_map.rfq_no);
								mobile.snd_msg = mobile.snd_msg.replace('$rfq_tit$', mobile_map.rfq_tit);
								mobile.clb_url = tmp_info.clb_url.replace('$rfq_no$', mobile_map.rfq_no);
		                    }

							$jdbc.update("unico", "insert.unico", mobile);
	                    }
	                    */
						// ******************************************************************************

						//발주테이블 저장
						if (smsMap.ret_val == "10"){
							if(list.get(i).typ == "PO"){
								$jdbc.update("procurement/po", "update.po.send", list.get(i));
							}else{
								$jdbc.update("procurement/po", "update.pn.send", list.get(i));
							}
						}

						smsMap.ret_val = "";
					}

					result.code = 'S';

	            }catch(e){

	                $jdbc.rollback();
	                var err_msg  = "";
	                var err      = e.toString();
	                var ora_err  = err.substring(err.indexOf("ORA"), err.length);

	                if ( strUtil.isEmpty(ora_err) )
	                   err_msg = err;
	                else
	                   err_msg = ora_err;

	                result.code = "E";
	                result.msg = "오류가 발생을 하였습니다. " + "\n" + "에러내용" + " : " + err_msg;
	            }

	            $data.result = result;

			]]>
		</script>
	</service>

	<service id="send.batch.po.delay.desc" label="납품지연사유 미입력 알림">
		<script>
			<![CDATA[
				var exmld         	= $xmlManager.getDescriptor("properties");
				var sms_send_no    	= exmld.getDescriptorElement("sms.send.iv.no").getText();
				var serverPath  	= exmld.getDescriptorElement("server.url").getText();    //메일  JSP 참조하기 위한 URL(WAS)
				var webserverPath   = exmld.getDescriptorElement("webserver.url").getText(); //메일 image URL(외부웹서버)
				var email_send_nm   = exmld.getDescriptorElement("email.send.nm").getText(); //메일 수신자명(세션값 없는경우)
				var email_send_ad   = exmld.getDescriptorElement("email.send.ad").getText(); //메일 수신자 메일주소(세션값 없는경우)

				var param = new Map();

				//메일 및 SMS보내는 대상 업체담당자 조회한다.
				var list = $jdbc.queryList("procurement/po", "select.batch.po.delay.desc.new", param);

				var udpClient = new UDPClient();

				for(var i=0; i < list.size(); i++){

					var userList = $jdbc.queryList("procurement/po", "get.vendor.person.list.vd_cd", list.get(i) );
					/*
					/////////////////////////////////////////////////////////////////////////////
					//SMS메세지 보내기
					/////////////////////////////////////////////////////////////////////////////
					*/
					var smsMap = new Map();

					/*
					    1.예약일자
						YYYYMMDDHHMMSS(년월일시분초)로 입력
						8자리 이하(YYYYMMDD)면 즉시 발송으로 처리
						9자리 이상(YYYYMMDDH)면 이후 값을 '0'으로 채원 예약처리
						숫자가 아닌 문자열이 있으면 오류처리.
					*/
					smsMap.send_dt = "";

					/*
					    2.발신자번호
						숫자와 '-'만 가능
					*/
					if (list.get(i).full_phone_no == null){
						// Default 송신자번호
						smsMap.send_no = sms_send_no;
					}
					else {
						smsMap.send_no = list.get(i).full_phone_no;
					}

					/*
					    4.메세지
						80byte 이내로 가능
					*/
					smsMap.sms_tmp_id = "S06";
					sendMessage = $jdbc.query("admin/template", "select.sms.template.info", smsMap);
					sendMessage.conts = sendMessage.conts.replace('%po_tit%', list.get(i).po_tit);
					sendMessage.conts = sendMessage.conts.replace('%comp_nm%', list.get(i).comp_nm);
					smsMap.message = sendMessage.conts;

					/*
					    5.로그인 사번
					*/
					smsMap.usr_id = $user.usr_id;

					/*
					    6.병원구분코드
					*/
					smsMap.comp_cd = list.get(i).comp_cd;
					var ret_val = "00";

					//발송여부가 'Y'인 경우만 발송
					if(sendMessage.use_yn == "Y"){

						$logger.info("userList.size() :" + userList.size());
						for(var iPerson = 0; iPerson < userList.size() ; iPerson++){
							smsMap.receive_no = userList.get(iPerson).send_chr_mobile;
							if(smsMap.receive_no != "" && smsMap.receive_no != null){
								if(sendMessage.use_yn == "Y"){
									ret_val = udpClient.sendToSMS(smsMap);
									$logger.info("Receive No :" + smsMap.receive_no + " // Result ====>" + ret_val);
								}
							}
						}

					}

					/*
					/////////////////////////////////////////////////////////////////////////////
					//E-mail batch에 등록하기
					/////////////////////////////////////////////////////////////////////////////
					*/
					var saveParam = $data.saveItem;
					var param = new Map();

					var comp_cd = $user.comp_cd;
					var mailContents = "";

					///////////////////////////////////////////////////////////////
					// 1. 메일 템플릿 정보를 가져온다.
					///////////////////////////////////////////////////////////////
					param.comp_cd_yn	= 'N';    // 병원별 템플릿이 동일 할 경우는 'N'
					param.mail_tmp_id   = 'T07';  // 납품지연알림

					mailContents = $jdbc.query("admin/template", "select.mail.template.info", param);

					var snd_title = "";
					var snd_subject = "";

					if( mailContents ) {
						snd_title    = mailContents.snd_title;
						snd_subject  = mailContents.snd_subject;

						// 제목
						snd_title = snd_title.replace('%po_tit%', list.get(i).po_tit);
						snd_title = snd_title.replace('%comp_nm%', list.get(i).comp_nm);

						// 내용
						snd_subject = snd_subject.replace('%po_no_list%', list.get(i).po_no_list);
					}

					///////////////////////////////////////////////////////////////
					// 2. EMAIL내용 준비
					///////////////////////////////////////////////////////////////

					if( mailContents ) {
						$data.mails         = new List();

						var mail = new Map();
						mail.from           = email_send_ad;
						mail.fromName       = email_send_nm;
						//mail.to             = list.get(i).chr_email;
						//mail.toName         = list.get(i).chr_nm;

						mail.snd_title      = snd_title;   //메일 제목
						mail.snd_subject    = snd_subject; //메일 본문
						mail.url            = serverPath + "/mail/templateMail.jsp"; //공통메일 템플릿
						mail.image_url      = webserverPath + "/mail/img/";  //메일  JSP 참조하기 위한 URL(WAS)
						//mail.comp_cd        = list.get(i).comp_cd;  //병원별 메일 템플릿 구분자
						mail.comp_cd        = '00';  //병원별 메일 템플릿 구분자

						$data.mails.add(mail);

						$logger.info('size----------------' + $data.mails.size());

						//발송여부가 'Y'인 경우만 이메일 발송
						if(mailContents.use_yn == "Y"){
							for(var y = 0 ; y < $data.mails.size(); y++){
								$data.mail = $data.mails[y];

								for(var iPerson = 0; iPerson < userList.size() ; iPerson++){
									mail.to             = userList.get(iPerson).chr_email;
									mail.toName         = userList.get(iPerson).chr_nm;

									$jdbc.update("admin/template", "insert.email", $data.mail);
								}

							}
						}
					}

					/*
					/////////////////////////////////////////////////////////////////////////////
					//E-mail batch에 등록하기
					/////////////////////////////////////////////////////////////////////////////
					*/

				}

			]]>
		</script>
	</service>

	<service id="send.batch.cntr.afis" label="단가계약 생성 배치(AFIS)">
		<script>
			<![CDATA[
				//AFIS 배치 조회
				var list = $jdbc.queryList("if", "select.contract.batch.afis", null);

				importClass(Packages.volcano.custom.interfacemodule.InterfaceSendService);
				var sendService = new InterfaceSendService();
				for(var i=0; i < list.size(); i++){
					// 1.단가계약 Interface 처리 ********************************************************
					var interfaceMap  = new Map();
					interfaceMap.put("comp_cd", list.get(i).comp_cd);
					interfaceMap.put("cntr_no", list.get(i).cntr_no);
					interfaceMap.put("cntr_rev", list.get(i).cntr_rev);
					interfaceMap.put("cntr_lno", list.get(i).cntr_lno);

					sendService.sendInterface_cr_afis(interfaceMap);
					// ******************************************************************************
				}
			]]>
		</script>
	</service>


	<service id="send.batch.cntr.afis.backup" label="단가계약 생성 배치(AFIS3.0)">
		<script>
			<![CDATA[
				//AFIS 배치 조회
				var list = $jdbc.queryList("common/interface", "renewal.select.contract.batch.afis", null);

				importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
				var sendService = new InterfaceSendProvider();
				for(var i=0; i < list.size(); i++){
					// 1.단가계약 Interface 처리 ********************************************************
					var interfaceMap  = new Map();
					interfaceMap.put("if_cd",    "IF1053");
					interfaceMap.put("comp_cd",  list.get(i).comp_cd);
					interfaceMap.put("cntr_no",  list.get(i).cntr_no);
					interfaceMap.put("cntr_rev", list.get(i).cntr_rev);
					interfaceMap.put("cntr_lno", list.get(i).cntr_lno);

					retMap = sendService.sendInterface(interfaceMap);

					if(retMap.get("log_sts") == "SUCCESS") {
						$jdbc.update("procurement/po", "renewal.update.contract.batch.ok.afis", interfaceMap);
					} else {
						$jdbc.update("procurement/po", "renewal.update.contract.batch.err.afis", interfaceMap);
					}
					// ******************************************************************************
				}
			]]>
		</script>
	</service>

	<service id="batch.kpi.result" label="KPI 배치 실행">
		<script>
			<![CDATA[
				var item = new Map();

				// 실적 삭제
				$jdbc.update("procurement/anal","delete.esmkp.data", null);

				// 실적 공통부분
				// 1. 예산대비절감액
				// 2. 예산대비절감율
				// 9. LEADTIME(평균) : 구매요청일자 ~ 발주일자, 구매요청일자 ~ 낙찰일자
				// GUBUN => A : 견적절감액(율), B : 단가계약 LEADTIME, C : 물품 입찰절감액(율)
				var strQuery = " ( ";
				strQuery += "     SELECT  DISTINCT POHD.SYS_ID, POHD.COMP_CD, POHD.PO_NO, POHD.PURC_ID, POHD.PO_DATE, ";
				strQuery += "             POHD.PO_TOT_AMT, PODT.PO_AMT, POHD.VD_CD, ";
				strQuery += "             PRHD.PR_DATE AS PR_DATE,  ";
				strQuery += "             PRDT.PR_NO, PRDT.PR_LNO, ";
				strQuery += "             'A' AS GUBUN ";
				strQuery += "       FROM  ESPPOHD POHD, ESPPODT PODT, ESPRQDT RQDT, ESPPRDT PRDT, ESPPRHD PRHD, ESAUSER USR ";
				strQuery += "      WHERE  1=1 ";
				strQuery += "      	 AND  (POHD.PO_DATE LIKE TO_CHAR(SYSDATE, 'YYYY') || '%'  ";
				strQuery += "             OR POHD.PO_DATE LIKE (TO_CHAR(SYSDATE, 'YYYY') - 1) || '%' ) ";
				strQuery += "        AND  POHD.SYS_ID     = PODT.SYS_ID ";
				strQuery += "        AND  POHD.COMP_CD    = PODT.COMP_CD ";
				strQuery += "        AND  POHD.PO_NO      = PODT.PO_NO ";
				strQuery += "        AND  PODT.SYS_ID     = RQDT.SYS_ID ";
				strQuery += " 		 AND  PODT.COMP_CD    = RQDT.COMP_CD ";
				strQuery += "        AND  PODT.PR_NO      = RQDT.PR_NO ";
				strQuery += "        AND  PODT.PR_LNO     = RQDT.PR_LNO ";
				strQuery += "        AND  PRDT.SYS_ID     = PRHD.SYS_ID ";
				strQuery += "        AND  PRDT.COMP_CD    = PRHD.COMP_CD ";
				strQuery += "        AND  PRDT.PR_NO      = PRHD.PR_NO ";
				strQuery += "        AND  PRDT.SYS_ID     = PODT.SYS_ID ";
				strQuery += "        AND  PRDT.COMP_CD    = PODT.COMP_CD ";
				strQuery += "        AND  PRDT.PR_NO      = PODT.PR_NO ";
				strQuery += "        AND  PRDT.PR_LNO     = PODT.PR_LNO ";
				strQuery += "        AND  POHD.SYS_ID	  = USR.SYS_ID ";
				strQuery += "        AND  POHD.PURC_ID    = USR.USR_ID ";
				strQuery += "        AND  POHD.PO_TYP	  = '2' ";
				strQuery += "        AND  POHD.CUR 		  = 'KRW' ";
				strQuery += "        AND  PRHD.PR_TYP     = '1' ";
				strQuery += "        AND  POHD.STS		  <> 'D' ";
				strQuery += "        AND  PODT.STS		  <> 'D' ";
				strQuery += "        AND  PRHD.STS		  <> 'D' ";
				strQuery += "        AND  PRDT.STS		  <> 'D' ";
				strQuery += "        AND  USR.BUYER_YN 	  = 'Y' ";
				strQuery += "       UNION ALL 																			  ";
				strQuery += "		SELECT DISTINCT SDHD.SYS_ID, SDHD.COMP_CD, SDHD.BID_NO, SDHD.PURC_ID, SDHD.BID_DATE,  ";
				strQuery += "		       SDHD.BID_AMT,  SDDT.ITEM_AMT, SDHD.VD_CD,                                      ";
				strQuery += "		       PRHD.PR_DATE,                                                                  ";
				strQuery += "		       PRDT.PR_NO, PRDT.PR_LNO,                                                       ";
				strQuery += "		       'B' AS GUBUN                                                                   ";
				strQuery += "		  FROM ESESDHD SDHD, ESESDDT SDDT, ESPPRDT PRDT, ESPPRHD PRHD, ESAUSER USR            ";
				strQuery += "		 WHERE 1=1                                                                            ";
				strQuery += "		   AND (SDHD.BID_DATE LIKE TO_CHAR(SYSDATE, 'YYYY') || '%'                               ";
				strQuery += "		        OR SDHD.BID_DATE LIKE (TO_CHAR(SYSDATE, 'YYYY') - 1) || '%' )                      ";
				strQuery += "		   AND SDHD.BID_DIV = 'Q'                                                                ";
				strQuery += "		   AND SDHD.CNTR_STS <> 'D'                                                              ";
				strQuery += "		   AND SDHD.STS <> 'D'                                                                   ";
				strQuery += "		   AND SDDT.STS <> 'D'                                                                   ";
				strQuery += "		   AND PRHD.PR_TYP = '2'                                                                 ";
				strQuery += "		   AND PRDT.STS <> 'D'                                                                   ";
				strQuery += "		   AND SDHD.SYS_ID = SDDT.SYS_ID                                                         ";
//				strQuery += "		   AND SDHD.COMP_CD = SDDT.COMP_CD                                                       ";
				strQuery += "		   AND SDHD.BID_NO = SDDT.BID_NO                                                         ";
				strQuery += "		   AND SDHD.VD_CD = SDDT.VD_CD                                                         ";
				strQuery += "		   AND SDDT.SYS_ID = PRDT.SYS_ID                                                         ";
				strQuery += "		   AND SDDT.COMP_CD = PRDT.COMP_CD                                                       ";
				strQuery += "		   AND SDDT.PR_NO = PRDT.PR_NO                                                           ";
				strQuery += "		   AND SDDT.PR_LNO = PRDT.PR_LNO                                                         ";
				strQuery += "		   AND PRDT.SYS_ID = PRHD.SYS_ID                                                         ";
				strQuery += "		   AND PRDT.COMP_CD = PRHD.COMP_CD                                                       ";
				strQuery += "		   AND PRDT.PR_NO = PRHD.PR_NO                                                           ";
				strQuery += "		   AND SDHD.SYS_ID = USR.SYS_ID                                                          ";
				strQuery += "		   AND SDHD.PURC_ID = USR.USR_ID                                                         ";
				strQuery += "		   AND USR.BUYER_YN = 'Y'                                                                ";
				strQuery += "       UNION ALL 																			  ";
				strQuery += "		SELECT DISTINCT SDHD.SYS_ID, SDHD.COMP_CD, SDHD.BID_NO, SDHD.PURC_ID, SDHD.BID_DATE,  ";
				strQuery += "		       SDHD.BID_AMT,  SDDT.ITEM_AMT, SDHD.VD_CD,                                      ";
				strQuery += "		       PRHD.PR_DATE,                                                                  ";
				strQuery += "		       PRDT.PR_NO, PRDT.PR_LNO,                                                       ";
				strQuery += "		       'C' AS GUBUN                                                                   ";
				strQuery += "		  FROM ESESDHD SDHD, ESESDDT SDDT, ESPPRDT PRDT, ESPPRHD PRHD, ESAUSER USR            ";
				strQuery += "		 WHERE 1=1                                                                            ";
				strQuery += "		   AND (SDHD.BID_DATE LIKE TO_CHAR(SYSDATE, 'YYYY') || '%'                               ";
				strQuery += "		        OR SDHD.BID_DATE LIKE (TO_CHAR(SYSDATE, 'YYYY') - 1) || '%' )                      ";
				strQuery += "		   AND SDHD.BID_DIV = 'B'                                                                ";
				strQuery += "		   AND SDHD.CNTR_STS <> 'D'                                                              ";
				strQuery += "		   AND PRHD.PURC_TYP = '1'                                                              ";
				strQuery += "		   AND SDHD.STS <> 'D'                                                                   ";
				strQuery += "		   AND SDDT.STS <> 'D'                                                                   ";
				strQuery += "		   AND PRDT.STS <> 'D'                                                                   ";
				strQuery += "		   AND SDHD.SYS_ID = SDDT.SYS_ID                                                         ";
//				strQuery += "		   AND SDHD.COMP_CD = SDDT.COMP_CD                                                       ";
				strQuery += "		   AND SDHD.BID_NO = SDDT.BID_NO                                                         ";
				strQuery += "		   AND SDHD.VD_CD = SDDT.VD_CD                                                         ";
				strQuery += "		   AND SDDT.SYS_ID = PRDT.SYS_ID                                                         ";
				strQuery += "		   AND SDDT.COMP_CD = PRDT.COMP_CD                                                       ";
				strQuery += "		   AND SDDT.PR_NO = PRDT.PR_NO                                                           ";
				strQuery += "		   AND SDDT.PR_LNO = PRDT.PR_LNO                                                         ";
				strQuery += "		   AND PRDT.SYS_ID = PRHD.SYS_ID                                                         ";
				strQuery += "		   AND PRDT.COMP_CD = PRHD.COMP_CD                                                       ";
				strQuery += "		   AND PRDT.PR_NO = PRHD.PR_NO                                                           ";
				strQuery += "		   AND SDHD.SYS_ID = USR.SYS_ID                                                          ";
				strQuery += "		   AND SDHD.PURC_ID = USR.USR_ID                                                         ";
				strQuery += "		   AND USR.BUYER_YN = 'Y'                                                                ";
				strQuery += " ) RESULT";
				item.strQuery = strQuery;

				// 실적 공통부분
				// 6. 단가계약건수
				// 7. 단가계약품목수
				// 8. 단가계약인하율(평균)
				var strQuery1 = " ( ";
				strQuery1 += "     SELECT SDHD.SYS_ID, SDHD.COMP_CD, SDHD.BID_NO, SDHD.VD_CD, SDHD.BID_DATE, ";
				strQuery1 += "            SDHD.BID_AMT, SDHD.PURC_ID, SDDT.ITEM_AMT, PRDT.PR_NO, PRDT.PR_LNO, ";
				strQuery1 += "            PRHD.PR_DATE, PRHD.PR_TYP, PRDT.CHG_ITEM_CD, PRDT.ERP_ITEM_CD, ";
				strQuery1 += "            '' AS BUDGET_AMT, 'A' AS GUBUN ";
				strQuery1 += "       FROM ESESDHD SDHD, ESESDDT SDDT, ESPPRDT PRDT, ESPPRHD PRHD, ESAUSER USR ";
				strQuery1 += "      WHERE 1=1 ";
				strQuery1 += "        AND (SDHD.BID_DATE LIKE TO_CHAR(SYSDATE, 'YYYY') || '%' ";
				strQuery1 += "      	   OR SDHD.BID_DATE LIKE (TO_CHAR(SYSDATE, 'YYYY') - 1) || '%' )  ";
				strQuery1 += "        AND SDHD.BID_DIV = 'Q' ";
				strQuery1 += "        AND SDHD.CNTR_STS <> 'D' ";
				strQuery1 += "        AND SDHD.STS <> 'D' ";
				strQuery1 += "        AND SDDT.STS <> 'D' ";
				strQuery1 += "        AND PRHD.PR_TYP = '2' ";
				strQuery1 += "        AND PRDT.STS <> 'D' ";
				strQuery1 += " 		  AND SDHD.SYS_ID = SDDT.SYS_ID ";
//				strQuery1 += "        AND SDHD.COMP_CD = SDDT.COMP_CD ";
				strQuery1 += "        AND SDHD.BID_NO = SDDT.BID_NO ";
				strQuery1 += "        AND SDHD.VD_CD = SDDT.VD_CD ";
				strQuery1 += "        AND SDDT.SYS_ID = PRDT.SYS_ID ";
				strQuery1 += "        AND SDDT.COMP_CD = PRDT.COMP_CD ";
				strQuery1 += "        AND SDDT.PR_NO = PRDT.PR_NO ";
				strQuery1 += "        AND SDDT.PR_LNO = PRDT.PR_LNO ";
				strQuery1 += "        AND PRDT.SYS_ID = PRHD.SYS_ID ";
				strQuery1 += "        AND PRDT.COMP_CD = PRHD.COMP_CD ";
				strQuery1 += "        AND PRDT.PR_NO = PRHD.PR_NO ";
				strQuery1 += "        AND SDHD.SYS_ID = USR.SYS_ID ";
				strQuery1 += "        AND SDHD.PURC_ID = USR.USR_ID ";
				strQuery1 += "        AND USR.BUYER_YN = 'Y' ";
				strQuery1 += "      UNION ALL	";
                strQuery1 += "     SELECT UPHD.SYS_ID, UPDT.COMP_CD, UPHD.UP_NO, UPDT.VD_CD, UPHD.UP_DT, ";
                strQuery1 += "		 	  0, UPHD.REG_ID, UPDT.ITEM_PRICE, '', '', 	";
                strQuery1 += "            '', '', UPDT.ERP_ITEM_CD, UPDT.ERP_ITEM_CD, ";
				strQuery1 += "            TO_CHAR(UPDT.BUDGET_AMT), 'B'						";
				strQuery1 += "       FROM ESEUPHD UPHD, ESEUPDT UPDT, ESAUSER USR	";
				strQuery1 += "      WHERE UPHD.SYS_ID = UPDT.SYS_ID					";
				strQuery1 += "        AND UPHD.UP_NO = UPDT.UP_NO					";
				strQuery1 += "        AND (UPHD.UP_DT LIKE TO_CHAR(SYSDATE, 'YYYY') || '%'	";
                strQuery1 += "            OR UPHD.UP_DT LIKE (TO_CHAR(SYSDATE, 'YYYY') - 1) || '%' )	";
				strQuery1 += "        AND UPHD.APRV_STS = '4'		";
				strQuery1 += "        AND UPHD.STS <> 'D'			";
				strQuery1 += "        AND UPDT.STS <> 'D'			";
				strQuery1 += "        AND UPHD.SYS_ID = USR.SYS_ID 	";
				strQuery1 += "        AND UPHD.REG_ID = USR.USR_ID 	";
				strQuery1 += "        AND USR.BUYER_YN = 'Y' 		";
				strQuery1 += " ) RESULT								";
				item.strQuery1 = strQuery1;

				// 1. 예산대비절감액-외화
				// 2. 예산대비절감율-외화
				var strQuery2 = " ( ";
				strQuery2 += "     SELECT  DISTINCT POHD.SYS_ID, POHD.COMP_CD, POHD.PO_NO, POHD.PURC_ID, POHD.PO_DATE, ";
				strQuery2 += "             POHD.PO_TOT_AMT, PODT.PO_AMT, POHD.VD_CD, ";
				strQuery2 += "             PRHD.PR_DATE AS PR_DATE,  ";
				strQuery2 += "             PRDT.PR_NO, PRDT.PR_LNO, ";
				strQuery2 += "             NVL(POHD.EXCH_RATE, 1) AS EXCH_RATE ";
				strQuery2 += "       FROM  ESPPOHD POHD, ESPPODT PODT, ESPRQDT RQDT, ESPPRDT PRDT, ESPPRHD PRHD, ESAUSER USR ";
				strQuery2 += "      WHERE  1=1 ";
				strQuery2 += "      	 AND  (POHD.PO_DATE LIKE TO_CHAR(SYSDATE, 'YYYY') || '%'  ";
				strQuery2 += "             OR POHD.PO_DATE LIKE (TO_CHAR(SYSDATE, 'YYYY') - 1) || '%' ) ";
				strQuery2 += "        AND  POHD.SYS_ID     = PODT.SYS_ID ";
				strQuery2 += "        AND  POHD.COMP_CD    = PODT.COMP_CD ";
				strQuery2 += "        AND  POHD.PO_NO      = PODT.PO_NO ";
				strQuery2 += "        AND  PODT.SYS_ID     = RQDT.SYS_ID ";
				strQuery2 += " 		  AND  PODT.COMP_CD    = RQDT.COMP_CD ";
				strQuery2 += "        AND  PODT.PR_NO      = RQDT.PR_NO ";
				strQuery2 += "        AND  PODT.PR_LNO     = RQDT.PR_LNO ";
				strQuery2 += "        AND  PRDT.SYS_ID     = PRHD.SYS_ID ";
				strQuery2 += "        AND  PRDT.COMP_CD    = PRHD.COMP_CD ";
				strQuery2 += "        AND  PRDT.PR_NO      = PRHD.PR_NO ";
				strQuery2 += "        AND  PRDT.SYS_ID     = PODT.SYS_ID ";
				strQuery2 += "        AND  PRDT.COMP_CD    = PODT.COMP_CD ";
				strQuery2 += "        AND  PRDT.PR_NO      = PODT.PR_NO ";
				strQuery2 += "        AND  PRDT.PR_LNO     = PODT.PR_LNO ";
				strQuery2 += "        AND  POHD.SYS_ID	   = USR.SYS_ID ";
				strQuery2 += "        AND  POHD.PURC_ID    = USR.USR_ID ";
				strQuery2 += "        AND  POHD.PO_TYP	   = '2' ";
				strQuery2 += "        AND  POHD.CUR 	   <> 'KRW' ";
				strQuery2 += "        AND  PRHD.PR_TYP     = '1' ";
				strQuery2 += "        AND  POHD.STS		  <> 'D' ";
				strQuery2 += "        AND  PODT.STS		  <> 'D' ";
				strQuery2 += "        AND  PRHD.STS		  <> 'D' ";
				strQuery2 += "        AND  PRDT.STS		  <> 'D' ";
				strQuery2 += "        AND  USR.BUYER_YN   =  'Y' ";
				strQuery2 += " ) RESULT";
				item.strQuery2 = strQuery2;

 				// 1. 예산대비절감액(합계) : 예산금액 - 낙찰(게약)금액
 				$jdbc.update("procurement/anal", "insert.esmkpdt.K01010100", item);

 				// 2. 예산대비절감율(월별) : 100 - (낙찰금액/예산금액) * 100
 				$jdbc.update("procurement/anal", "insert.esmkpdt.K01010200", item);

				// 2-1. 예산대비절감율(년별) : 100 - (낙찰금액/예산금액) * 100
				$jdbc.update("procurement/anal", "insert.esmkpdt.K01010200.year", item);

				// 2-2. 예산대비절감액(합계) - 외화 : 예산금액 - 낙찰(게약)금액
				$jdbc.update("procurement/anal", "insert.esmkpdt.K01010300", item);

				// 2-3. 예산대비절감율(월별) - 외화 : 100 - (낙찰금액/예산금액) * 100
				$jdbc.update("procurement/anal", "insert.esmkpdt.K01010400", item);

				// 2-4. 예산대비절감율(년별) - 외화: 100 - (낙찰금액/예산금액) * 100
				$jdbc.update("procurement/anal", "insert.esmkpdt.K01010400.year", item);

				// 3. 발주건수(합계) : ESPPOHD
				$jdbc.update("procurement/anal", "insert.esmkpdt.K01020100", item);

				// 3-1. 발주건수(합계)-외자 : ESPPOHD
				$jdbc.update("procurement/anal", "insert.esmkpdt.K01020200", item);

				// 4. 협력업체신규발굴수(합계) : 자동 배치 제외(2015-02-11)
				// $jdbc.update("procurement/anal", "insert.esmkpdt.K02010100", item);

				// 5. 대체품목발주건수(합계)
				$jdbc.update("procurement/anal", "insert.esmkpdt.K02020100", item);

				// 6. 단가계약건수
				$jdbc.update("procurement/anal", "insert.esmkpdt.K03040100", item);

				// 7. 단가계약품목수
				$jdbc.update("procurement/anal", "insert.esmkpdt.K03040101", item);

				// 8. 단가계약인하율(월별평균)
				$jdbc.update("procurement/anal", "insert.esmkpdt.K03040102", item);

				// 8-1. 단가계약인하율(년별평균)
				$jdbc.update("procurement/anal", "insert.esmkpdt.K03040102.year", item);

				// 9. LEADTIME(월별평균) : 구매요청일자 ~ 발주일자
				$jdbc.update("procurement/anal", "insert.esmkpdt.K03040300", item);

				// 9-1. LEADTIME(년별평균) : 구매요청일자 ~ 발주일자
				$jdbc.update("procurement/anal", "insert.esmkpdt.K03040300.year", item);

				// KPI 실적 Insert
				$jdbc.update("procurement/anal", "merge.esmkphd", item);

			]]>
		</script>
	</service>

	<service id="batch.kpi.result.backup" label="KPI 배치 실행">
		<script>
			<![CDATA[
				var item = new Map();

				// 실적 삭제
				$jdbc.update("procurement/anal","delete.esmkp.data", null);

				// 실적 공통부분
				// 1. 예산대비절감액
				// 2. 예산대비절감율
				// 6. 단가계약품목수
				// 7. LEADTIME
				var strQuery = " ( ";
				strQuery += "     SELECT SDHD.SYS_ID, SDHD.COMP_CD, SDHD.BID_NO, SDHD.VD_CD, SDHD.BID_DATE, ";
				strQuery += "            SDHD.BID_AMT, SDHD.PURC_ID, SDDT.ITEM_AMT, PRDT.PR_NO, PRDT.PR_LNO, ";
				strQuery += "            PRDT.PR_DATE, PRHD.PR_TYP, PRDT.CHG_ITEM_CD, PRDT.ERP_ITEM_CD ";
				strQuery += "       FROM ESESDHD SDHD, ESESDDT SDDT, ESPPRDT PRDT, ESPPRHD PRHD, ESAUSER USR ";
				strQuery += "      WHERE 1=1 ";
				strQuery += "        AND (SDHD.BID_DATE LIKE TO_CHAR(SYSDATE, 'YYYY') || '%' ";
				strQuery += "      		  OR SDHD.BID_DATE LIKE (TO_CHAR(SYSDATE, 'YYYY') - 1) || '%' )  ";
				strQuery += "        AND SDHD.BID_DIV = 'Q' ";
				strQuery += "        AND SDHD.CNTR_STS <> 'D' ";
				strQuery += "        AND SDHD.STS <> 'D' ";
				strQuery += "        AND SDDT.STS <> 'D' ";
				strQuery += "        AND PRDT.STS <> 'D' ";
				strQuery += " 		 AND SDHD.SYS_ID = SDDT.SYS_ID ";
//				strQuery += "        AND SDHD.COMP_CD = SDDT.COMP_CD ";
				strQuery += "        AND SDHD.BID_NO = SDDT.BID_NO ";
				strQuery += "        AND SDDT.SYS_ID = PRDT.SYS_ID ";
				strQuery += "        AND SDDT.COMP_CD = PRDT.COMP_CD ";
				strQuery += "        AND SDDT.PR_NO = PRDT.PR_NO ";
				strQuery += "        AND SDDT.PR_LNO = PRDT.PR_LNO ";
				strQuery += "        AND PRDT.SYS_ID = PRHD.SYS_ID ";
				strQuery += "        AND PRDT.COMP_CD = PRHD.COMP_CD ";
				strQuery += "        AND PRDT.PR_NO = PRHD.PR_NO ";
				strQuery += "        AND SDHD.SYS_ID = USR.SYS_ID ";
				strQuery += "        AND SDHD.PURC_ID = USR.USR_ID ";
				strQuery += "        AND USR.BUYER_YN = 'Y' ";
				strQuery += " ) RESULT";
				item.strQuery = strQuery;

				// 1. 예산대비절감액(합계) : 예산금액 - 낙찰(게약)금액
				$jdbc.update("procurement/anal", "insert.esmkpdt.K01010100", item);

				// 2. 예산대비절감율(평균) : 100 - (낙찰금액/예산금액) * 100
				$jdbc.update("procurement/anal", "insert.esmkpdt.K01010200", item);

				// 3. 발주건수(합계) : ESPPOHD
				$jdbc.update("procurement/anal", "insert.esmkpdt.K01020100", item);

				// 4. 협력업체신규발굴수(합계)
				$jdbc.update("procurement/anal", "insert.esmkpdt.K02010100", item);

				// 5. 대체품목발주건수(합계)
				$jdbc.update("procurement/anal", "insert.esmkpdt.K02020100", item);

				// 6. 단가계약품목수
				$jdbc.update("procurement/anal", "insert.esmkpdt.K03040100", item);

				// 7. LEADTIME(평균) : 구매요청일자 ~ 낙찰일자
				$jdbc.update("procurement/anal", "insert.esmkpdt.K03040300", item);

				// KPI 실적 Insert
				$jdbc.update("procurement/anal", "merge.esmkphd", item);

			]]>
		</script>
	</service>

	<service id="send.batch.3pl.iv" label="3PL 납품정보 전송 배치">
		<script>
			<![CDATA[
                var retMap = new Map();
                importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
                var sendService = new InterfaceSendProvider();
                var sendCompleteInvno  = new Map();
                var findString = "해당 납품 건은 기 납품되었습니다";

                $logger.debug("=================[send.batch.3pl.iv BATCH_START]=================");

                // 3PL 발주접수정보/납품정보 배치 조회
                var list = $jdbc.queryList("common/interface", "renewal.3pl.send.batch.espiv", null);

                $logger.debug("=================[renewal.3pl.send.batch.espiv queryList Count: " + list.size()  +" ]=================");

                for(var i=0; i < list.size(); i++){

                    // 100건씩 끊어서 하도록 추가
                    if(i >= 500)
                    {
                        break;
                    }
                    // 3PL 발주접수정보/납품정보 Interface 처리 ********************************************************
                    var interfaceMap  = new Map();
                    interfaceMap.put("if_cd",   "IF1202");  // IF1093 => IF1202로 변경(관리로 인한 변경)
                    interfaceMap.put("comp_cd", list.get(i).comp_cd);
                    interfaceMap.put("inv_no",  list.get(i).inv_no);

                    if (list.get(i).batch_typ == "IV"){

                        $logger.debug("=================[sendService.sendInterface 시작]=================");
                        retMap = sendService.sendInterface(interfaceMap);

                        list[i].log_sts = retMap.get("log_sts");
                        list[i].message = retMap.get("message");

                        $logger.debug("=================[sendService.sendInterface 끝]=================");
                    }

                    // 3pl 납품일련번호 Interface 처리
                    if(list.get(i).sn_cnt > 0) {

                        $logger.debug("=================[3pl 납품일련번호 Interface 처리 시작]=================");
                        var interfaceMap1  = new Map();
                        interfaceMap1.put("if_cd",   "IF1204");  // IF1160 => IF1202로 변경(관리로 인한 변경)
                        interfaceMap1.put("comp_cd", list.get(i).comp_cd);
                        interfaceMap1.put("inv_no",  list.get(i).inv_no);
                        sendService.sendInterface(interfaceMap1);

                        $logger.debug("=================[3pl 납품일련번호 Interface 처리 끝]=================");
                    }
                    // ******************************************************************************
                }

                $logger.debug("=================[renewal.update.espivhd.iv.sendOk.com 시작]=================");

                // 2024-08-29 이창민 - 배치 로직 변경 (IF 완료 후 UPDATE 하도록 개선, 기납품된 납품번호는 납품완료처리 하도록 로직 추가)
                for(var i=0; i < list.size(); i++){

                    // 100 건씩 끊어서 하도록 추가
                    if(i >= 100)
                    {
                        break;
                    }

                    if(list[i].log_sts == "SUCCESS" || list[i].message.indexOf(findString) > 0) {

                        sendCompleteInvno.put("comp_cd", list.get(i).comp_cd);
                        sendCompleteInvno.put("inv_no",  list.get(i).inv_no);
                        $logger.debug(' INV_NO + LOG_STAT **  =====> ' + list[i].inv_no + ' : ' + list[i].log_sts);
                        $jdbc.update("common/interface", "renewal.update.espivhd.iv.sendOk.com", sendCompleteInvno);
                    }
                }

                $logger.debug("=================[renewal.update.espivhd.iv.sendOk.com 끝]=================");

                $logger.debug("=================[send.batch.3pl.iv BATCH_END]=================");
			]]>
		</script>
	</service>

	<service id="send.batch.3pl.po.01" label="3PL 가납접수정보  전송 배치">
		<script>
			<![CDATA[

				// 3PL 발주접수정보/납품정보 배치 조회
				var list = $jdbc.queryList("common/interface", "renewal.3pl.send.batch.esppo.01", null);

				importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
				var sendService = new InterfaceSendProvider();

				for(var i=0; i < list.size(); i++){
					// 3PL 발주접수정보/납품정보 Interface 처리 ********************************************************
					var interfaceMap  = new Map();
					interfaceMap.put("if_cd", "IF1113");
					interfaceMap.put("comp_cd", list.get(i).comp_cd);
					interfaceMap.put("inv_no", list.get(i).inv_no);
					interfaceMap.put("po_no", list.get(i).po_no);

					if (list.get(i).batch_typ == "PO" && list.get(i).po_typ == "4"){
						sendService.sendInterface(interfaceMap);
					}
					// ******************************************************************************
				}

			]]>
		</script>
	</service>

	<service id="send.batch.3pl.po.02" label="3PL 발주접수정보  전송 배치">
		<script>
			<![CDATA[
				// 3PL 발주접수정보/납품정보 배치 조회
				var list = $jdbc.queryList("common/interface", "renewal.3pl.send.batch.esppo.02", null);
				var strpolno    = "";

				if(list.size() > 0) strpolno = "(";

				var j=0;
				var s_pt="N";

				for(var i=0; i<list.size(); i++){
					var element = list.get(i);

					if(j>999) {
					    strpolno += ") or DT.PO_NO||'L'||DT.PO_LNO in (";
					    j    = 0;
					    s_pt = "Y";
					} else {
						if (strpolno != "(") strpolno+= ",";
					}

					strpolno += "'"+element.po_no+"L"+element.po_lno+"'";

					j++;
				}
				if(s_pt == "Y") strpolno += ") ";
				if(list.size() > 0) strpolno += ")";

				importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
				var sendService = new InterfaceSendProvider();
				interfaceMap.put("if_cd",    "IF1112");
				interfaceMap.put("comp_cd",  element.comp_cd);
				interfaceMap.put("strpolno", strpolno);
                sendService.sendInterface(interfaceMap);

				/* -- 일괄접수로 변경함.
				for(var i=0; i < list.size(); i++){
					// 3PL 발주접수정보/납품정보 Interface 처리 ********************************************************
					var interfaceMap  = new Map();
					interfaceMap.put("if_cd", "IF1113");
					interfaceMap.put("comp_cd", list.get(i).comp_cd);
					interfaceMap.put("inv_no", list.get(i).inv_no);
					interfaceMap.put("po_no", list.get(i).po_no);

					if (list.get(i).batch_typ == "PO" && list.get(i).po_typ == "1"){
						sendService.sendInterface(interfaceMap);
					}
					// ******************************************************************************
				}
				*/
			]]>
		</script>
	</service>

	<service id="send.batch.lab.iv" label="연구소 납품정보 전송 배치">
		<script>
			<![CDATA[
				var retMap = new Map();
				// 연구소 발주접수정보/납품정보 배치 조회
				var list = $jdbc.queryList("common/interface", "renewal.lab.send.batch.espiv", null);

				importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
				var sendService = new InterfaceSendProvider();

				for(var i=0; i < list.size(); i++){
					// 연구소 납품정보 Interface 처리 ********************************************************
					var interfaceMap  = new Map();
					interfaceMap.put("if_cd",   "IF1190");
					interfaceMap.put("comp_cd", list.get(i).comp_cd);
					interfaceMap.put("inv_no",  list.get(i).inv_no);

					if (list.get(i).batch_typ == "IV"){
						retMap = sendService.sendInterface(interfaceMap);

						if(retMap.get("log_sts") == "SUCCESS") {
							$jdbc.update("common/interface", "renewal.update.espivhd.iv.sendOk.com", interfaceMap);
						} else {
							$jdbc.update("common/interface", "renewal.update.espivhd.iv.error.com", interfaceMap);
						}
					}
					// ******************************************************************************
				}

			]]>
		</script>
	</service>

	<service id="send.batch.lab.pr.01" label="연구소 접수.미접수.반송 송신 IF">
		<script>
			<![CDATA[
				// 연구소 발주접수정보/납품정보 배치 조회
				//var list = $jdbc.queryList("common/interface", "renewal.lab.select.espprdt.reject_yn.01", null);

				importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
				var sendService = new InterfaceSendProvider();

				// 연구소 발주접수정보/접수취소/반품 Interface
				var interfaceMap  = new Map();
				interfaceMap.put("if_cd",   "IF1188");
				sendService.sendInterface(interfaceMap);
			]]>
		</script>
	</service>

	<service id="send.batch.lab.po.prog.recv" label="연구소 발주진행상태IF">
		<script>
			<![CDATA[
				importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
				var sendService = new InterfaceSendProvider();

				// 연구소 발주진행상태(업체진행상태) Interface
				var interfaceMap  = new Map();
				interfaceMap.put("if_cd",   "IF1189");
				sendService.sendInterface(interfaceMap);
			]]>
		</script>
	</service>

	<service id="send.batch.lab.iv.reg" label="연구소 납품등록IF">
		<script>
			<![CDATA[
				importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
				var sendService = new InterfaceSendProvider();

				// 연구소 납품등록 Interface
				var interfaceMap  = new Map();
				interfaceMap.put("if_cd",   "IF1190");
				sendService.sendInterface(interfaceMap);
			]]>
		</script>
	</service>

	<service id="send.batch.lab.iv.cancel" label="연구소 납품취소IF">
		<script>
			<![CDATA[
				importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
				var sendService = new InterfaceSendProvider();

				// 연구소 납품등록 Interface
				var interfaceMap  = new Map();
				interfaceMap.put("comp_cd", "10");
				interfaceMap.put("if_cd",   "IF1191");
				sendService.sendInterface(interfaceMap);
			]]>
		</script>
	</service>

	<service id="send.batch.food.iv" label="현대그린푸드 납품정보 전송 배치">
		<script>
			<![CDATA[
				var retMap = new Map();
				// 현대그린푸드 발주접수정보/납품정보 배치 조회
				var list = $jdbc.queryList("common/interface", "renewal.food.send.batch.espiv", null);

				importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
				var sendService = new InterfaceSendProvider();

				for(var i=0; i < list.size(); i++){
					// 3PL 발주접수정보/납품정보 Interface 처리 ********************************************************
					var interfaceMap  = new Map();
					interfaceMap.put("if_cd",   "IF1171");   // IF1171(납품)
					interfaceMap.put("comp_cd", list.get(i).comp_cd);
					interfaceMap.put("inv_no",  list.get(i).inv_no);

					if (list.get(i).batch_typ == "IV"){
						retMap = sendService.sendInterface(interfaceMap);

						if(retMap.get("log_sts") == "SUCCESS") {
							$jdbc.update("common/interface", "renewal.update.espivhd.iv.sendOk.com", interfaceMap);
						} else {
							$jdbc.update("common/interface", "renewal.update.espivhd.iv.error.com", interfaceMap);
						}
					}
					// ******************************************************************************
				}

			]]>
		</script>
	</service>

	<service id="vd.trans.mdm.proc" label="[MDM] 거래처 승인내역 이관 배치">
		<script>
			<![CDATA[
				var resultMap = new Map();
				var transCompSize = 0;
				var param = new Map();
				importPackage(Packages.emro.util);
				importPackage(Packages.volcano.custom.interfacemodule);
				var util = new SHA512Util();
				var reject_cause = ""; //반려사유
				var temp = new Map();
				var send_yes = ""; //업체SMS대상 확인
				var exmld         	= $xmlManager.getDescriptor("properties");
				var sms_send_no    	= exmld.getDescriptorElement("sms.send.no").getText();
				var serverPath  	= exmld.getDescriptorElement("server.url").getText();    //메일  JSP 참조하기 위한 URL(WAS)
				var webserverPath   = exmld.getDescriptorElement("webserver.url").getText(); //메일 image URL(외부웹서버)
				var email_send_nm   = exmld.getDescriptorElement("email.send.nm").getText(); //메일 수신자명(세션값 없는경우)
				var email_send_ad   = exmld.getDescriptorElement("email.send.ad").getText(); //메일 수신자 메일주소(세션값 없는경우)

				//거래처 승인내역 이관 대상 조회
				var paramList = $jdbc.queryList("common/interface", "renewal.select.eai_mdm_vd_app", null);

				for(var i=0; i < paramList.size(); i++){
					var paramMap = $jdbc.query("common/interface", "renewal.select.eai_mdm_vd_app.map", paramList.get(i));

					try {

						if(paramMap.aprval_stat_cd == "30") {
							//거래처 승인내역 이관 처리1 - ESPTEPB merge
							$jdbc.update("common/interface", "renewal.merge.esptepb", paramMap);
						}

						//거래처 승인내역 이관 처리 후처리	= EAI_MDM_VD_APP trans_yn = 'Y'
						$jdbc.update("common/interface", "renewal.update.eai_mdm_vd_app", paramMap);

						//업체거래상태변경로직추가
						//1.paramList오브젝트에 TRNSC_PS_YN = 'Y'인 경우에만 ESMVDHT히스토리테이블에 INSERT 처리
						// apros에서 관리하는 업체애 대해서 처리
						if(( paramMap.vd_sn != null && paramMap.vd_sn != "" ) && paramMap.trnsc_ps_yn == "Y"){
							var vdInfo = $jdbc.query("esourcing/sql-vendor", "select.vd.using.biz.reg.no", paramMap);
							$jdbc.update("esourcing/sql-vendor", "insert.vd.deal.sts.history", vdInfo);
						}
						System.out.println("=====> "+paramMap);
						//신규거래처 승인인 경우
						if(( paramMap.vd_sn != null && paramMap.vd_sn != "" ) && paramMap.new_vd_cd_yn == "Y"){

							if(paramMap.aprval_stat_cd != "30") {
								//업체반려시
								$jdbc.update("stdm/stdm", "save.new.reject.list",  paramMap);

								try {
								    reject_cause = paramMap.givback_rsn;
								    var brno = paramMap.brno;
								    var actcust_nm = paramMap.actcust_nm;

								    var userinfos = $jdbc.queryList("stdm/stdm", "get.latest.send.userinfo.eai.mdm.vd.req", paramMap);

								    var mailContents = "";
								    var comp_cd = "10";

								    var mailParam = new Map();
								    mailParam.put("sys_id", "EMRO");
								    mailParam.put("mail_tmp_id", "T25");
								    mailParam.put("comp_cd", comp_cd);
								    mailParam.put("comp_cd_yn", "N");
								    mailParam.put("mail_type", "B");

								    mailContents = $jdbc.query("admin/template", "select.mail.template.info", mailParam);

								    var snd_title = "";
								    var snd_subject = "";

								    if (mailContents) {
								        snd_title   = mailContents.snd_title;
								        snd_subject = mailContents.snd_subject;

								        snd_subject = snd_subject.replace('%reject_cause%', reject_cause);
								        snd_subject = snd_subject.replace('%brno%', brno);
								        snd_subject = snd_subject.replace('%actcust_nm%', actcust_nm);
								    }

								    if (mailContents && mailContents.use_yn == "Y") {
								        for (var z = 0; z < userinfos.size(); z++) {
								            var mail = new Map();
								            mail.from        = userinfos.get(z).chr_email;
								            mail.fromName    = userinfos.get(z).chr_nm;
								            mail.to          = userinfos.get(z).chr_email;
								            mail.toName      = userinfos.get(z).chr_nm;
								            mail.snd_title   = snd_title;
								            mail.snd_subject = snd_subject;
								            mail.url         = serverPath + "/mail/templateMail.jsp";
								            mail.image_url   = webserverPath + "/mail/img/";
								            mail.sys_id      = mailParam.get("sys_id");
								            mail.comp_cd     = mailParam.get("comp_cd");
								            mail.mail_type   = mailParam.get("mail_type");

								            if (mail.to != "" && mail.to != null) {
								                $jdbc.update("admin/template", "insert.email", mail);
								            }
								        }
								    }
								} catch (mailErr) {
								    $logger.error("[MAIL][T25][NEW_REJECT] send fail. vd_sn=" + paramMap.vd_sn + " / err=" + mailErr);
								}finally

							} else {
								// 업체승인시
								// 임시->정식권한부여
								var send_yes = "Y"; //업체SMS대상 확인
								var biz_reg_no = paramMap.biz_reg_no;
								paramMap.usr_id = biz_reg_no;
								paramMap.role_cd = 'SMASTER';

								$jdbc.update("admin/user", "insert.user.role", paramMap);

								$jdbc.update("stdm/stdm", "save.new.accept.list",  paramMap);

								// 강릉ERP 서울병원이 승인되었고 업체가 강릉병원도 요청하였다면 강릉병원에 승인처리
								if(paramMap.gn_mdm_targ_yn == "Y" && paramMap.new_gn_vd_cd_yn == "Y"){

									var gn_inter = new InterfaceSendProvider();
									var reqNo = $jdbc.query("common/interface", "select.reqno.info", null).req_no;

									var param3 = new Map();
									param3.vd_sn = paramMap.vd_sn;
									param3.if_cd = "IF4022";
									param3.if_typ = "GNERP";
									param3.req_no = reqNo;
									param3.req_mode = "I";
									param3.auto_aprv_yn = "Y";
									//신규업체요청정보 MDM전송
									var result = gn_inter.sendInterface(param3);

									var param4 = new Map();
									param4.vd_sn = paramMap.vd_sn;
									param4.if_cd = "IF4080";
									param4.att_typ = "VDFILE";
									param4.if_typ = "GNERP";
									param4.req_no = reqNo;
									param4.req_mode = "I";

									//신규업체파일정보 MDM전송
									var result2 = gn_inter.sendInterface(param4);
								}


								//AFIS 3.0 MDM 인터페이스 시작 ****************************************************
								var nSysCnt = $jdbc.query("common/interface", "renewal.select.list.n_sys_cnt", null);
								// AFIS 3.0 오픈을 했으면 AFIS3.0 과 I/F 함
								if ( nSysCnt.get("n_sys_cnt") > 0 ) {
									//importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
									var sendService = new InterfaceSendProvider();

									var interfaceMap  = new Map();
									interfaceMap.put("if_cd" , "IF1023");
									interfaceMap.put("if_typ", "AFIS");
									interfaceMap.put("vd_sn" , paramMap.vd_sn);

									sendService.sendInterface(interfaceMap);
								}
								//AFIS 3.0 MDM 인터페이스 종료 ****************************************************
							}

						//변경승인인 경우
						}else if(( paramMap.vd_sn != null && paramMap.vd_sn != "" ) && paramMap.eai_recv_side_task_typ_cd == "U"){

							if(paramMap.aprval_stat_cd != "30") {//업체반려시 류태준 추가
								//ESMVDGL 반려로 업데이트
								$jdbc.update("stdm/stdm", "save.new.reject.list",  paramMap);

								try{
									//공급업체 정보변경 요청건 반려
									reject_cause = paramMap.givback_rsn;
									var brno = paramMap.brno;
									var actcust_nm = paramMap.actcust_nm;

									//반려정보 저장
									var userinfos = $jdbc.queryList("stdm/stdm", "get.latest.send.userinfo.eai.mdm.vd.req",paramMap); //eai_mdm_vd_req최근 건의 담당자로 설정

									// 0. 메일전송
									var mailContents = "";
									var comp_cd = "10";

									// 1. 메일 템플릿 정보를 가져온다.
									var mailParam = new Map();
					                mailParam.put("sys_id", "EMRO");
									mailParam.put("mail_tmp_id", "T25");   // 공급사 정보변경요청 반려
									mailParam.put("comp_cd", comp_cd);
									mailParam.put("comp_cd_yn", "N");      // 병원별 템플릿 동일
									mailParam.put("mail_type", "B");

									mailContents = $jdbc.query("admin/template", "select.mail.template.info", mailParam);

									var snd_title = "";
									var snd_subject = "";

									///////////////////////////////////////////////////////////////
									// 2. EMAIL내용 준비
									///////////////////////////////////////////////////////////////

									if( mailContents ) {
										snd_title    = mailContents.snd_title;
										snd_subject  = mailContents.snd_subject;
										snd_subject  = snd_subject.replace('%reject_cause%', reject_cause);
										snd_subject  = snd_subject.replace('%brno%', brno);
										snd_subject  = snd_subject.replace('%actcust_nm%', actcust_nm);

									}
									if( mailContents ) {
										$data.mails         = new List();
										for(var z = 0; z < userinfos.size() ; z++){
											var mail = new Map();
											mail.from 			= userinfos.get(z).chr_email;
											mail.fromName       = userinfos.get(z).chr_nm;
											mail.to             = userinfos.get(z).chr_email;
											mail.toName         = userinfos.get(z).chr_nm;
											mail.snd_title      = snd_title;   							 //메일 제목
											mail.snd_subject    = snd_subject; 							 //메일 본문
											mail.url            = serverPath + "/mail/templateMail.jsp"; //공통메일 템플릿
											mail.image_url      = webserverPath + "/mail/img/";  		 //메일  JSP 참조하기 위한 URL(WAS)
											mail.sys_id    		= mailParam.get("sys_id");
											mail.comp_cd   		= mailParam.get("comp_cd");
											mail.mail_type 		= mailParam.get("mail_type");

											if(mail.to != "" && mail.to != null){
												$data.mails.add(mail);
											}
										}
										$logger.info('size----------------' + $data.mails.size());
										//발송여부가 'Y'인 경우만 이메일 발송
										if(mailContents.use_yn == "Y"){
											for(var y = 0 ; y < $data.mails.size(); y++){
												$data.mail = $data.mails[y];
												$jdbc.update("admin/template", "insert.email", $data.mail);
											}
										}
									}
								}catch (mailErr) {
								    $logger.error("[MAIL][T25][REJECT] send fail. vd_sn=" + paramMap.vd_sn + " / err=" + mailErr);
								}
							} else {
								//업체승인시
								//거래처 승인내역 이관 처리2 - ESMVDGL update(RQ)
								$jdbc.update("common/interface", "renewal.update.esmvdgl", paramMap);

								//승인정보 업데이트
								$jdbc.update("esourcing/sql-vendor", "aprove.vd.chg.info",  paramMap);


								// 강릉ERP 서울병원이 승인되었고 업체가 강릉병원도 요청하였다면 강릉병원에 요청
								if(paramMap.gn_mdm_targ_yn == "Y" && paramMap.new_gn_vd_cd_yn == "N"){
									//importPackage(Packages.volcano.custom.interfacemodule);
									var gn_inter = new InterfaceSendProvider();

									var reqNo = $jdbc.query("common/interface", "select.reqno.info", null).req_no;

									var param3 = new Map();
									param3.vd_sn = paramMap.vd_sn;
									param3.if_cd = "IF4022";
									param3.if_typ = "GNERP";
									param3.req_no = reqNo;
									param3.req_mode = "U";
									param3.auto_aprv_yn = "Y";
									//신규업체요청정보 MDM전송
									var result = gn_inter.sendInterface(param3);

									var param4 = new Map();
									param4.vd_sn = paramMap.vd_sn;
									param4.if_cd = "IF4080";
									param4.att_typ = "VDFILE";
									param4.if_typ = "GNERP";
									param4.req_no = reqNo;
									param4.req_mode = "U";

									//신규업체파일정보 MDM전송
									var result2 = gn_inter.sendInterface(param4);
								}


								//AFIS 3.0 MDM 인터페이스 시작 ****************************************************
								var nSysCnt = $jdbc.query("common/interface", "renewal.select.list.n_sys_cnt", null);
								// AFIS 3.0 오픈을 했으면 AFIS3.0 과 I/F 함
								if ( nSysCnt.get("n_sys_cnt") > 0 ) {

									//importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
									var sendService = new InterfaceSendProvider();

									var interfaceMap  = new Map();
									interfaceMap.put("if_cd" , "IF1023");
									interfaceMap.put("if_typ", "AFIS");
									interfaceMap.put("vd_sn" , paramMap.vd_sn);

									sendService.sendInterface(interfaceMap);
								}
								//AFIS 3.0 MDM 인터페이스 종료 ****************************************************


								//기준정보로 승인요청된것만 알림처리(서울승인요청(SO_MDM_CHK_RES_CD가 P) 또는 강릉승인요청(GN_MDM_CHK_RES_CD가 P인) 경우
								if (( paramMap.SO_MDM_CHK_RES_CD == "P" )|| ( paramMap.GN_MDM_CHK_RES_CD == "P" )){
				 					//SMS 받을 대상 목록(업체 담당자들) 조회
									var send_yes = $jdbc.queryList("login", "vendor_aprov_yn", paramMap);
									if (send_yes == "Y") {
										var userinfos = $jdbc.queryList("login", "external.valid.usr.list", paramMap);

										var udpClient = new UDPClient();
										var smsMap = new Map();

										smsMap.sms_tmp_id = "S09";
										smsMap.sys_id = param.sys_id;

										var sendMessage = $jdbc.query("admin/template", "select.sms.template.info", smsMap);
										smsMap.message = sendMessage.conts;
										smsMap.send_no = sms_send_no;
										smsMap.usr_id = 'APROS';
										smsMap.comp_cd = '00';
										var ret_val = '';
										for(var j = 0; j < userinfos.size() ; j++){
											smsMap.send_dt = userinfos.get(j).sms_send_dt;
											smsMap.receive_no = userinfos.get(j).chr_mobile;
											if(smsMap.receive_no != "" && smsMap.receive_no != null){
												if(sendMessage.use_yn == "Y"){
													ret_val = udpClient.sendToSMS(smsMap);
												}
											}
					                   	}

										// 0. 메일전송
										var comp_cd = "10";
										var mailContents = "";

										// 1. 메일 템플릿 정보를 가져온다.
										param.comp_cd = comp_cd;
										param.comp_cd_yn	= 'N';    // 병원별 템플릿이 동일 할 경우는 'N'
										param.mail_tmp_id   = 'T17';  // 공급사 정보변경요청 승인

										mailContents = $jdbc.query("admin/template", "select.mail.template.info", param);

										var snd_title = "";
										var snd_subject = "";

										///////////////////////////////////////////////////////////////
										// 2. EMAIL내용 준비
										///////////////////////////////////////////////////////////////

										if( mailContents ) {
											snd_title    = mailContents.snd_title;
											snd_subject  = mailContents.snd_subject;
										}
										if( mailContents ) {
											$data.mails         = new List();
											for(var z = 0; z < userinfos.size() ; z++){
												var mail = new Map();
												mail.from 			= email_send_ad;
												mail.fromName       = email_send_nm;
												mail.to             = userinfos.get(z).chr_email;
												mail.toName         = userinfos.get(z).chr_nm;
												mail.snd_title      = snd_title;   							 //메일 제목
												mail.snd_subject    = snd_subject; 							 //메일 본문
												mail.url            = serverPath + "/mail/templateMail.jsp"; //공통메일 템플릿
												mail.image_url      = webserverPath + "/mail/img/";  		 //메일  JSP 참조하기 위한 URL(WAS)
												mail.comp_cd        = comp_cd;  //병원별 메일 템플릿 구분자

												if(mail.to != "" && mail.to != null){
													$data.mails.add(mail);
												}
											}
											$logger.info('size----------------' + $data.mails.size());
											//발송여부가 'Y'인 경우만 이메일 발송
											if(mailContents.use_yn == "Y"){
												for(var y = 0 ; y < $data.mails.size(); y++){
													$data.mail = $data.mails[y];
													$jdbc.update("admin/template", "insert.email", $data.mail);
												}
											}
										}
									}
								}
							}
						}
						resultMap.put("log_sts" , "SUCCESS");
						resultMap.put("err_msg" , "");

					}
					catch(e) {
						var err      = e.toString();
						var ora_err  = err.substring(err.indexOf("ORA"), err.length);
						rtrn_msg     = "처리시 에러가 발생을 하였습니다." + "\n" + "에러내용" + " : " + ora_err;
						resultMap.put("log_sts"   , "FAIL"     );
						resultMap.put("err_msg"    , rtrn_msg);
					}

					//인터페이스(EAI_MDM*->업무테이블) 로그 추가
					var dt = new Date();
					var today = dt.getFullYear()+ ((dt.getMonth() + 1).toString()<10?("0" + (dt.getMonth()+1)).toString():(dt.getMonth()+1).toString()) + (dt.getDate()<10?("0"+dt.getDate()).toString():dt.getDate().toString());
					resultMap.tx_id = today + Math.floor(Math.random() * 99999999);
					resultMap.job_key = "{ vd_sn : '"+paramMap.vd_sn + "'}";
					if(paramList.size() == 0){
						resultMap.req_data = "";
					} else {
						resultMap.req_data = paramList.get(i).toString();
					}
					resultMap.res_data = "";
					resultMap.if_nm = "거래처승인내역";
					resultMap.if_cd = "IF1019";

					$logger.info("## resultMap:"+resultMap);
					//$jdbc.update("common/interface","merge.interface.send.log", resultMap);
					$jdbc.update("common/interface","merge.interface.send.log.insert", resultMap);

				}
			]]>
		</script>
	</service>}


	<service id="place.trans.mdm.proc" label="[MDM] 장소 이관 배치">
		<script>
			<![CDATA[
				var resultMap = new Map();
				var transCompSize = 0;
				var param = new Map();
				// [MDM] 장소사용 승인내역 이관 대상 조회
				var list = $jdbc.queryList("common/interface", "renewal.select.eai_mdm_place_app", null);

				for(var i=0; i < list.size(); i++){

					try {

						$jdbc.update("common/interface", "renewal.p.update.eai_mdm_place_app", list.get(i));

						if(list.get(i).eai_send_typ_cd != 'M06U'){

							// M06M은 장소지번 매핑임. MDM에서 연계시스템별로 각각 작업을 할 수 없어 일괄적으로 동일하게 I/F 하고 있어 예외처리함, APROS 3.0에서는 M06U 만 처리함.
							$jdbc.update("common/interface", "renewal.update.eai_mdm_place_app", list.get(i));

						}else{
							// [MDM] 장소사용 승인내역 이관 처리 ********************************************************
							var transYn = $jdbc.update("common/interface", "renewal.merge.esaware", list.get(i));
							// *********************************************************************************
							if(transYn == 1){
								// [MDM] 장소사용 승인내역 이관 처리 후처리
								$jdbc.update("common/interface", "renewal.update.eai_mdm_place_app", list.get(i));
							}
						}

						resultMap.put("log_sts" , "SUCCESS");
						resultMap.put("err_msg" , "");

					}
					catch(e) {
						var err      = e.toString();
						var ora_err  = err.substring(err.indexOf("ORA"), err.length);
						rtrn_msg     = "처리시 에러가 발생을 하였습니다." + "\n" + "에러내용" + " : " + ora_err;
						resultMap.put("log_sts"   , "FAIL"     );
						resultMap.put("err_msg"    , rtrn_msg);
					}

					//인터페이스(EAI_MDM*->업무테이블) 로그 추가
					var dt = new Date();
					var today = dt.getFullYear()+ ((dt.getMonth() + 1).toString()<10?("0" + (dt.getMonth()+1)).toString():(dt.getMonth()+1).toString()) + (dt.getDate()<10?("0"+dt.getDate()).toString():dt.getDate().toString());
					resultMap.tx_id = today + Math.floor(Math.random() * 99999999);
					resultMap.job_key = "{ deli_cd : '"+list.get(i).loc_use_cd + "'}";
					if(list.size() == 0){
						resultMap.req_data = "";
					} else {
						resultMap.req_data = list.get(i).toString();
					}
					resultMap.res_data = "";
					resultMap.if_nm = "장소사용 승인내역";
					resultMap.if_cd = "IF1009";

					$logger.info("## resultMap:"+resultMap);
					//$jdbc.update("common/interface","merge.interface.send.log", resultMap);
					$jdbc.update("common/interface","merge.interface.send.log.insert", resultMap);

				}
			]]>
		</script>
	</service>

	<service id="save.mapped.item_cd" label="매핑된 품번 갱신로직">
		<script>
			<![CDATA[
				var tepaList = $jdbc.queryList("masterItem","select.mtgl.default.list",new Map());

				$jdbc.batch("masterItem", "merge.mtgl.apply", tepaList);
				$jdbc.batch("masterItem", "update.tepa.apply", tepaList);

				$jdbc.update("masterItem", "update.item_cd.prdt.podt", new Map());
			]]>
		</script>
	</service>

	<service id="save.rfq.status" label="매핑된 품번 갱신로직">
		<script>
			<![CDATA[
				var rfqList = $jdbc.queryList("qt/qt","select.rfq.status.batch",new Map());

				$jdbc.batch("qt/qt", "update.rfq.status.batch", rfqList);
			]]>
		</script>
	</service>

	<service id="save.purchase.progress.info" label="구매진행현황 정보 갱신">
		<script>
			<![CDATA[
				var prList = $jdbc.queryList("procurement/anal","select.espprdt.batch.target.list",new Map());

				$jdbc.batch("procurement/anal", "merge.espprin.purchase.progress", prList);

			]]>
		</script>
	</service>

	<service id="save.common.base.usr.info" label="사용자, 부서, 겸직 정보 수신(공통)">
		<script>
			<![CDATA[
				importPackage(Packages.volcano.custom.interfacemodule);
				importPackage(Packages.emro.util);

				var param = new Map();
				param.put("if_cd","IF1001");

				var inter = new InterfaceSendProvider();
				inter.sendInterface(param);
			]]>
		</script>
	</service>

		<service id="save.common.base.usr.info2" label="사용자, 부서, 직위, 직책, 겸직 정보 수신(GW) 수동수신">
		<script>
			<![CDATA[
				var searchParam = $data.searchParam;

				importPackage(Packages.volcano.custom.interfacemodule);
				importPackage(Packages.emro.util);

				var param = new Map();
				param.put("if_cd","IF1001");
				param.put("finl_updt_dtm", searchParam.finl_updt_dtm);

				var inter = new InterfaceSendProvider();
				inter.sendInterface(param);
			]]>
		</script>
	</service>


	<service id="save.gw.base.usr.info" label="사용자, 부서, 직위, 직책, 겸직 정보 수신(GW)">
		<script>
			<![CDATA[
				importPackage(Packages.volcano.custom.interfacemodule);
				importPackage(Packages.emro.util);

				var param = new Map();
				param.put("if_cd","IF1002");

				var inter = new InterfaceSendProvider();
				inter.sendInterface(param);
			]]>
		</script>
	</service>

	<service id="esmmcls.trans.mdm" label="[MDM] 물품(품목)분류 배치">
		<script>
			<![CDATA[
				var param = new Map();
				var resultMap = new Map();

				// I/F제외대상 처리
				$jdbc.update("common/interface", "renewal.n.update.eai_mdm_common_cd_rcv", null);

				// [MDM] 물품(품목)분류 (Interface Tbl) 조회
				var list = $jdbc.queryList("common/interface", "renewal.select.eai_mdm_common_cd_rcv", null);

				for(var i=0; i < list.size(); i++){

					try {

						// 진행상태 = P
						$jdbc.update("common/interface", "renewal.p.update.eai_mdm_common_cd_rcv", list.get(i));

						// [MDM] 물품분류이관 처리 ********************************************************
						var transYn = $jdbc.update("common/interface", "renewal.merge.eai_mdm_common_cd_rcv", list.get(i));

						//$jdbc.update("common/interface", "renewal.merge.esmmcls_mod", list.get(i));   // cate_path, cate_en_path, up_date_id
						// ******************************************************************************

						if(transYn == 1){
							// [MDM] 거래처 승인내역 이관 처리 후처리
							$jdbc.update("common/interface", "renewal.e.update.eai_mdm_common_cd_rcv", list.get(i));
						}

						resultMap.put("log_sts" , "SUCCESS");
						resultMap.put("err_msg" , "");

					}
						catch(e) {
						var err      = e.toString();
						var ora_err  = err.substring(err.indexOf("ORA"), err.length);
						rtrn_msg     = "처리시 에러가 발생을 하였습니다." + "\n" + "에러내용" + " : " + ora_err;
						resultMap.put("log_sts"   , "FAIL"     );
						resultMap.put("err_msg"    , rtrn_msg);
					}

					//인터페이스(EAI_MDM*->업무테이블) 로그 추가
					var dt = new Date();
					var today = dt.getFullYear()+ ((dt.getMonth() + 1).toString()<10?("0" + (dt.getMonth()+1)).toString():(dt.getMonth()+1).toString()) + (dt.getDate()<10?("0"+dt.getDate()).toString():dt.getDate().toString());
					resultMap.tx_id = today + Math.floor(Math.random() * 99999999);
					resultMap.job_key = "{ cate_id : '"+list.get(i).cd_val_id + "'}";
					if(list.size() == 0){
						resultMap.req_data = "";
					} else {
						resultMap.req_data = list.get(i).toString();
					}
					resultMap.res_data = "";
					resultMap.if_nm = "물품분류정보";
					resultMap.if_cd = "IF1162";

					$logger.info("## resultMap:"+resultMap);
					//$jdbc.update("common/interface","merge.interface.send.log", resultMap);
					$jdbc.update("common/interface","merge.interface.send.log.insert", resultMap);

				}
			]]>
		</script>
	</service>

	<service id="send.batch.sms" label="Quartz를 통한 문자 발송">
		<script>
			<![CDATA[
				var list = $jdbc.queryList("admin/template", "batch.sms.list", null);

				var udpClient = new UDPClient();

				$logger.info("문자 발송 대상건수 >>  :: " + list.size());

				for(var i=0; i < list.size(); i++){
					var smsMap = list.get(i);
					ret_val = udpClient.sendToSMSPO(smsMap);
					smsMap.ret_val = ret_val;
					$logger.info("## ret_val:"+ret_val);

					$jdbc.update("admin/template", "batch.update.sms", smsMap);
					$jdbc.commit();
				}
			]]>
		</script>
	</service>

	<service id="delete.esielog.info" label="인터페이스 로그 삭제(2개월분만 남김)/2.0(1개월)">
		<script>
			<![CDATA[
				// 한달 남기고 모두 삭제
				$jdbc.update("common/interface", "delete.esielog.info", null);
				$jdbc.update("common/interface", "delete.interface_log.info", null);
			]]>
		</script>
	</service>

	<service id="att.folder.check.service" label="폴더생성">
		<script>
			<![CDATA[
				importPackage(Packages.volcano.custom.attachment);

				$logger.debug("Start Data Folder Check**********************************");
				var folder = new AttachFolderCheck();
				folder.diskFolderVolumeCheck();
				$logger.debug("End Data Folder Check**********************************");

			]]>
		</script>
	</service>

	<service id="batch.sg.mt.vd.mapping" label="소싱그룹물품업체자동매핑">
		<script>
			<![CDATA[

				// 소싱그룹 물품자동매핑 처리
				$jdbc.update("common/interface", "insert.esrsgma.info", null);

				// 소싱그룹 업체자동매핑 처리
				$jdbc.update("common/interface", "insert.esrsgvd.info", null);

			]]>
		</script>
	</service>

	<service id="batch.modify.vendor.attach.info" label="MDM협력업체파일전송플래그변경">
		<script>
			<![CDATA[

				// 협력업체 파일전송완료(EAI_ATTACH_FILE)건에 대해서
				// EAI_MDM_VD_FILE_REQ.EAI_TIES_STAT_CD의 값을 R에서 N으로 변경
				$jdbc.update("common/interface", "update.eai.mdm.vd.file.req.stat", null);

			]]>
		</script>
	</service>

	<service id="send.batch.3pl.po.recv" label="3PL/그린푸드 발주접수정보 전송 배치">
		<script>
			<![CDATA[
				var retMap = new Map();
				var err_msg = "";
				// 3PL/그린푸드 발주접수(발주수신건)정보 조회
				var list = $jdbc.queryList("common/interface", "renewal.3pl.send.batch.po.recv", null);

				importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
				var sendService = new InterfaceSendProvider();

				var strQuery = "AND HD.PO_NO IN (";
				var poQuery = "";

				for(var i=0; i < list.size(); i++){
					poQuery += "'";
					poQuery += list.get(i).po_no;
					poQuery += "'";
					poQuery += ",";
				}
				poQuery += "'X')";
				strQuery += poQuery;

				// 송신대상건이 있는 경우
				if(list.size() > 0){
					var interfaceMap  = new Map();
					interfaceMap.put("if_cd",   "IF1201");
					interfaceMap.put("comp_cd", list.get(0).comp_cd);
					interfaceMap.put("vd_cd", list.get(0).vd_cd);
					interfaceMap.put("strQuery", strQuery);

					retMap = sendService.sendInterface(interfaceMap);

					$logger.debug("retMap.get('log_sts')**********************************"+retMap.get("log_sts"));

					if(retMap.get("log_sts") == "SUCCESS") {
						$jdbc.update("common/interface", "renewal.update.esppodt.send", interfaceMap);
					// 2022-04-27 정홍근 추가 오류일경우 재전송 대상 제외처리 update 처리
					} else if(retMap.get("log_sts") == "FAIL"){
						var err_msg = "'" + retMap.get("err_msg") + "'" ;
						var interfaceErrMap  = new Map();

						interfaceErrMap.put("comp_cd", list.get(0).comp_cd);
						interfaceErrMap.put("vd_cd", list.get(0).vd_cd);
						interfaceErrMap.put("err_msg", err_msg);
						$logger.info("")
	                    $logger.debug("err_msg === "+err_msg);
	                    $logger.info("err_msg-info === "+err_msg);

	                    $jdbc.update("common/interface", "recv_yn_update.update.esppodt.send", interfaceErrMap);
					} else {
						$logger.info("");
					}
				}

				// 2024-09-05 이창민 - detail이 모두 Y 일 경우 head도 업데이트 하도록 로직 추가
				$jdbc.update("common/interface", "renewal.update.esppohd.recvyn", null);

			]]>
		</script>
	</service>


	<service id="save.common.ftp.test" label="FTP 테스트">
		<script>
			<![CDATA[
				importClass(Packages.volcano.custom.interfacemodule.InterfaceGnFTPAgency);
				var ftp = new InterfaceGnFTPAgency("1", "2");
				ftp.testFunction();
				ftp.disconnect();
			]]>
		</script>
	</service>

	<service id="save.common.ftp.test.down" label="FTP 테스트">
		<script>
			<![CDATA[
				importClass(Packages.volcano.custom.interfacemodule.InterfaceGnFTPAgency);
				var ftp = new InterfaceGnFTPAgency("1", "2");
				ftp.testFunctionDown();
				ftp.disconnect();
			]]>
		</script>
	</service>


	<service id="save.common.ftp.recv.test" label="FTP 테스트">
		<script>
			<![CDATA[
				importClass(Packages.volcano.custom.interfacemodule.InterfaceGnCallback);
				var calltest = new InterfaceGnCallback();
				//calltest.testFunction();
				calltest.testFunction_throw();
			]]>
		</script>
	</service>



	<service id="batch.gn.self.ord.y.send.iv" label="강릉아산병원자체발주납품서전송">
		<script>
			<![CDATA[
				$logger.info("::::::::::batch.gn.self.ord.y.send.iv start");
				var list = $jdbc.queryList("admin/template", "select.gn.self.ord.p", null);


				for( var i = 0 ; i < list.size() ;  i++ ){
					var obj = list[i];

					$logger.info("#!!!!!!!!!!!!!!!!!!!!!!!!!!!!!");
					$logger.info(obj);
					$logger.info("#!!!!!!!!!!!!!!!!!!!!!!!!!!!!!");

					$jdbc.update("procurement/po_vd", "update.podt.recv.auto", obj);

					importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
					var sendService = new InterfaceSendProvider();

					var interfaceMap  = new Map();

					/* 발주상태 인터페이스 : IF4113 */
					interfaceMap  = new Map();

					var poLnoList = $jdbc.queryList("procurement/po", "select.po.lno.list", obj);

					var strpolno = "(";

					for(var y = 0; y <poLnoList.size(); y++){
						if(y != 0){
							strpolno += ",";
						}
						strpolno += "'"+obj.comp_cd+"L"+poLnoList[y].pono_lno+"'";
					}
					strpolno += ")";

					interfaceMap.put("comp_cd",  obj.comp_cd);
					interfaceMap.put("if_typ",    "GNERP");
					interfaceMap.put("if_cd",    "IF4113");
					interfaceMap.put("strpolno", strpolno);

					sendService.sendInterface(interfaceMap);

					/* 발주상태 인터페이스 : IF4093 */
					// 납품번호 생성
					var inv_no = $helper.generateCode('IV');
					obj.put("inv_no", inv_no);

					// 납품상세 Insert
					$jdbc.update("procurement/po", "insert.espivdt.auto", obj);

					// 납품헤더 Insert
					$jdbc.update("procurement/po", "insert.espivhd.auto", obj);

					// 발주상세 Update
					$jdbc.update("procurement/po", "update.esppodt.auto", obj);

					// 납품 Interface 처리
					var interfaceMap1  = new Map();


				    interfaceMap1.put("if_typ",    "GNERP");
					interfaceMap1.put("if_cd",    "IF4093");

				    interfaceMap1.put("comp_cd", obj.comp_cd);
					interfaceMap1.put("inv_no", inv_no);
					sendService.sendInterface(interfaceMap1);


					$jdbc.update("procurement/po", "update.self.ord.send.yn.gn", obj);

				}


				$logger.info("::::::::::batch.gn.self.ord.y.send.iv end");
			]]>
		</script>
	</service>
	<!--
	<service id="batch.send.asannet.vender.update.info" label="협력업체 수정요청 담당자 알림(매일)">
		<script>
			<![CDATA[
				var list = $jdbc.queryList("admin/template", "select.vender.update.info", null);



				for( var i = 0 ; i < list.size() ;  i++ ){
					var mailMap = list[i];
					var itemCont = "";
				}



							var mailList = $jdbc.queryList("cntr", "select.mail.detail.info", $data.param);
							var itemList = $jdbc.queryList("cntr", "select.mod.cntrDt.info", $data.param);
			 				var fUtil 	 = new FtlUtil();


							//단가계약요청자 및 적정진료팀 여철민님(하드코딩으로 요청:구매팀 강성규차장) 원내메일 발송
							for(var i=0; i < mailList.size(); i++){
								var mailMap = mailList[i];
								var itemCont = "";

								mailMap.comp_cd_yn	= 'N';      		// 병원별 템플릿이 동일 할 경우는 'N'
								mailMap.mail_tmp_id = 'T11';  			// 단가계약요청 계약시 의뢰자 알림
								mailMap.itemList    = new List();		// 초기화

								for(var z=0;z<itemList.size();z++){
									if(mailMap.pr_no == itemList.get(z)){
										mailMap.itemList.add(itemList.get(z));
									}
								}

								mailContents = $jdbc.query("admin/template", "select.mail.template.info", mailMap);
								itemCont = fUtil.process("cntr_items.html",mailMap);

								var snd_title = "";
								var snd_subject = "";

								if( mailContents ) {
									snd_title    = mailContents.snd_title;
									snd_subject  = mailContents.snd_subject;

									// 내용
									snd_subject = snd_subject.replace('%pr_no%', mailMap.pr_no);
									snd_subject = snd_subject.replace('%pr_tit%', mailMap.pr_tit);
									snd_subject = snd_subject.replace('%cntr_no%', mailMap.cntr_no);
									snd_subject = snd_subject.replace('%cntr_tit%', mailMap.cntr_nm);
									snd_subject = snd_subject.replace('%vd_nm%', mailMap.vd_nm);
									snd_subject = snd_subject.replace('[[LIST]]', itemCont);

									// 메일 제목 및 내용
									mailMap.snd_title 		= snd_title;
									mailMap.snd_subject 	= snd_subject;
									mailMap.url 			= "";
									mailMap.image_url 		= "";
									mailMap.from 			= mailMap.send_id;
									mailMap.fromName		= mailMap.send_usr_nm;
									mailMap.to 				= mailMap.receiver_id;
									mailMap.toName 			= mailMap.receiver_usr_nm;
									mailMap.mail_type 		= "B";

									$jdbc.update("admin/template", "insert.email", mailMap); //원내메일 발송
								}
							}


			]]>
		</script>
	</service>
	-->

	<!--
	<service id="batch.gn.file.info.download.TEST" label="강릉아산병원자체발주납품서전송">
		<script>
			<![CDATA[
				$logger.info("::::::::::batch.gn.file.info.download start");

				/*
				importClass(Packages.volcano.custom.interfacemodule.InterfaceGnCallback);
				var call = new InterfaceGnCallback();
				call.gnFileDownload();

				// 첨부파일 저장 경로... data01, data02 늘어나니까 해줘야함
				//AttachmentAgency attInfo = new AttachmentAgency();
				//String aprosFilePath = replaceFilePath(attInfo.getUploadRootPath("comm"));
				//
				*/

				importClass(Packages.volcano.custom.interfacemodule.InterfaceGnCallback);
				importClass(Packages.volcano.custom.interfacemodule.InterfaceGnFTPAgency);
				importClass(Packages.volcano.custom.attachment.AttachmentAgency);

				var selectList = $jdbc.queryList( "common/interface_gn" , "select.gn.file.info.sts.n", null);

				if(selectList != null && selectList.size() > 0) {
					var call = new InterfaceGnCallback();
					var attInfo = new AttachmentAgency();
					var aprosFilePath = call.replaceFilePath(attInfo.getUploadRootPath("comm"));

					$logger.info("###!@#!@#!@#!@#");
					$logger.info(aprosFilePath);
					$logger.info("###!@#!@#!@#!@#");


					for(var i=0; i<selectList.size(); i++){
						var header = selectList[i];
						$jdbc.update("common/interface_gn", "update.gn.file.info.sts.p", header);
					}

					$jdbc.update("common/interface_gn", "renewal.grouping.esagnfi.gn", null);

					// 강릉 게이트웨이 FTP에서 파일 다운로드 시작
					var fileList = $jdbc.queryList( "common/interface_gn" , "renewal.select.esagnfi.info.list", null);

					var ftp = null;
					var ftpCheck = true;

					try {
						// FTP 접속 및 로그인
						ftp = new InterfaceGnFTPAgency();
						var loginRet = ftp.ftpLogin();

						if("S".equals(loginRet)) {
							ftpCheck = true;
						} else {
							ftpCheck = false;
							//return result;
						}
					} catch(e) {
						e.printStackTrace();
						ftpCheck = false;
					}

					if(!ftpCheck) {
						$logger.info("Error File Gn Download Batch");
					}

					for(var i=0; i<fileList.size(); i++){
						var fileInfo = fileList[i];
						var hostDir = fileInfo.get("att_file_path");
						var fileName = fileInfo.get("att_file_nm");

						var chk = ftp.ftpDownloadFile(hostDir, fileName, aprosFilePath);

						fileInfo.put("att_path_info", aprosFilePath);

						if(chk) {
							$jdbc.update("common/interface_gn", "renewal.insert.esaatth.gn.gateway.file.info", fileInfo);

							$jdbc.update("common/interface_gn", "update.gn.file.info.sts.g", fileInfo);
						}
					}




					try {
						if(ftp != null) {
							ftp.disconnect();
						}
					}catch(e) {
						$logger.info("Error File Gn Download Batch 2");
					}

					// 첨부파일목록 가져오기
					var fileGroups = $jdbc.queryList( "common/interface_gn" , "renewal.select.esagnfi.temp.gn", null);

					for(var i=0; i<fileGroups.size(); i++){
						var fileGroup = fileGroups[i];

						fileGroup.put("comp_cd", "80");
						// 업무단 업데이트 처리 로직 넣기
						//A:물품
						if("A" == fileGroup.get("job_typ")){
							//01-이미지
							if("01" == fileGroup.get("att_cls_typ")){
								fileGroup.put("img_att_no", fileGroup.get("grp_cd"));
							}
							//02-용도설명서
							else if("02" == fileGroup.get("att_cls_typ")){
								fileGroup.put("ag_att_no", fileGroup.get("grp_cd"));
							}
							//03-수입품목허가증
							else if("03" == fileGroup.get("att_cls_typ")){
								fileGroup.put("lcs_att_no", fileGroup.get("grp_cd"));
							}
							//04-소독제사용방법분류표
							else if("04" == fileGroup.get("att_cls_typ")){
								fileGroup.put("use_att_no", fileGroup.get("grp_cd"));
							}
							//99-기타
							else if("99" == fileGroup.get("att_cls_typ")){
								fileGroup.put("att_no", fileGroup.get("grp_cd"));
							}
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.casea.gn", fileGroup);
						}
						//B:구매요청HD
						else if("B" == fileGroup.get("job_typ")){
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.caseb.gn", fileGroup);
						}
						//C:단가계약요청HD
						else if("C" == fileGroup.get("job_typ")){
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.casec.gn", fileGroup);
						}
						//D:구매요청DT
						else if("D" == fileGroup.get("job_typ")){
							fileGroup.put("job_attr_1", fileGroup.get("job_attr_1")+"A");  // pr_no = job_attr_1 + 'A'
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.cased.gn", fileGroup);
						}
						//E:단가계약요청DT
						else if("E"==fileGroup.get("job_typ")){
							//단가계약번호 플래그 추가 2018.05.17
							fileGroup.put("job_attr_1", fileGroup.get("job_attr_1")+"B");  // pr_no = job_attr_1 + 'B'
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.casee.gn", fileGroup);
						}
						//F:신규물품심의
						else if("F"==fileGroup.get("job_typ")){
							//05-F:견적서
							if("05"==fileGroup.get("att_cls_typ")){
								fileGroup.put("req_att_no", fileGroup.get("grp_cd"));
							}
							//09-F:카탈로그
							if("09"==fileGroup.get("att_cls_typ")){
								fileGroup.put("cat_att_no", fileGroup.get("grp_cd"));
							}
							//03-수입품목허가증
							else if("03"==fileGroup.get("att_cls_typ")){
								fileGroup.put("lcs_att_no", fileGroup.get("grp_cd"));
							}
							//11-소모품정보
							else if("11"==fileGroup.get("att_cls_typ")){
								fileGroup.put("sub_item_att_no", fileGroup.get("grp_cd"));
							}
							//01-이미지
							else if("01"==fileGroup.get("att_cls_typ")){
								fileGroup.put("img_att_no", fileGroup.get("grp_cd"));
							}
							//13-용도설명서
							else if("13"==fileGroup.get("att_cls_typ")){
								fileGroup.put("uses_att_no", fileGroup.get("grp_cd"));
							}
							//14-식약처허가증
							else if("14"==fileGroup.get("att_cls_typ")){
								fileGroup.put("mfds_att_no", fileGroup.get("grp_cd"));
							}
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.casef.gn", fileGroup);
						}
						//G:불량진료재료
						else if("G"==fileGroup.get("job_typ")){
							// ESMMTBD에 update 로직 추가
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.mmtbd.gn", fileGroup);
						}

						//H:계약요청(공사)
						else if("H"==fileGroup.get("job_typ")){
							fileGroup.put("job_attr_1", fileGroup.get("job_attr_1")+"C");  // pr_no = job_attr_1 + 'C'
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.prdtc.gn", fileGroup);
						}
					}


					// 강릉 게이트웨이 FTP에서 파일 다운로드 시작
					var fileOkList = $jdbc.queryList( "common/interface_gn" , "renewal.select.esagnfi.info.list.g", null);

					for(var i=0; i<fileOkList.size(); i++){
						var fileInfo = fileOkList[i];

						$jdbc.update("common/interface_gn", "update.gn.file.info.sts.y", fileInfo);
					}



				}


				$logger.info("::::::::::batch.gn.file.info.download end");
			]]>
		</script>
	</service>
	-->


	<service id="batch.gn.file.info.download.real" label="강릉병원파일수신">
		<script>
			<![CDATA[
				$logger.info("::::::::::batch.gn.file.info.download start");

				/*
				importClass(Packages.volcano.custom.interfacemodule.InterfaceGnCallback);
				var call = new InterfaceGnCallback();
				call.gnFileDownload();

				// 첨부파일 저장 경로... data01, data02 늘어나니까 해줘야함
				//AttachmentAgency attInfo = new AttachmentAgency();
				//String aprosFilePath = replaceFilePath(attInfo.getUploadRootPath("comm"));
				//
				*/

				importClass(Packages.volcano.custom.interfacemodule.InterfaceGnCallback);
				importClass(Packages.volcano.custom.interfacemodule.InterfaceGnFTPAgency);
				importClass(Packages.volcano.custom.attachment.AttachmentAgency);

				var selectList = $jdbc.queryList( "common/interface_gn" , "select.gn.file.info.sts.list.n", null);

				if(selectList != null && selectList.size() > 0) {
					var call = new InterfaceGnCallback();
					var attInfo = new AttachmentAgency();
					var aprosFilePath = call.replaceFilePath(attInfo.getUploadRootPath("comm"));

					$logger.info("###!@#!@#!@#!@#");
					$logger.info(aprosFilePath);
					$logger.info("###!@#!@#!@#!@#");


					for(var i=0; i<selectList.size(); i++){
						var header = selectList[i];
						//$jdbc.update("common/interface_gn", "update.gn.file.info.sts.p", header);
						$jdbc.update("common/interface_gn", "update.gn.file.info.esaatth.sts.p", header);

					}

					//$jdbc.update("common/interface_gn", "renewal.grouping.esagnfi.gn", null);
					$jdbc.update("common/interface_gn", "renewal.grouping.esaatth.gn", null);


					// 강릉 게이트웨이 FTP에서 파일 다운로드 시작
					var fileList = $jdbc.queryList( "common/interface_gn" , "renewal.select.esaatth.info.list", null);

					var ftp = null;
					var ftpCheck = true;

					try {
						// FTP 접속 및 로그인
						ftp = new InterfaceGnFTPAgency();
						var loginRet = ftp.ftpLogin();

						if("S".equals(loginRet)) {
							ftpCheck = true;
						} else {
							ftpCheck = false;
							//return result;
						}
					} catch(e) {
						e.printStackTrace();
						ftpCheck = false;
					}

					if(!ftpCheck) {
						$logger.info("Error File Gn Download Batch");
						for(var i=0; i<fileList.size(); i++){
							var header = fileList[i];
							$jdbc.update("common/interface_gn", "update.gn.file.info.esaatth.sts.f", header);

						}
					}

					for(var i=0; i<fileList.size(); i++){
						var fileInfo = fileList[i];
						var hostDir = fileInfo.get("att_file_path");
						var fileName = fileInfo.get("att_file_nm");

						var chk = ftp.ftpDownloadFile(hostDir, fileName, aprosFilePath);

						fileInfo.put("att_path_info", aprosFilePath);

						if(chk) {
							$jdbc.update("common/interface_gn", "renewal.insert.esaatth.gn.gateway.file.info", fileInfo);

							$jdbc.update("common/interface_gn", "update.gn.file.info.esaatth.sts.g", fileInfo);
						} else{
							$jdbc.update("common/interface_gn", "update.gn.file.info.esaatth.sts.f", fileInfo);
						}
					}




					try {
						if(ftp != null) {
							ftp.disconnect();
						}
					}catch(e) {
						$logger.info("Error File Gn Download Batch 2");
					}

					// 첨부파일목록 가져오기
					var fileGroups = $jdbc.queryList( "common/interface_gn" , "renewal.select.esaatth.temp.gn", null);

					for(var i=0; i<fileGroups.size(); i++){
						var fileGroup = fileGroups[i];

						fileGroup.put("comp_cd", "80");
						// 업무단 업데이트 처리 로직 넣기
						//A:물품
						if("A" == fileGroup.get("job_typ")){
							//01-이미지
							if("01" == fileGroup.get("att_cls_typ")){
								fileGroup.put("img_att_no", fileGroup.get("grp_cd"));
							}
							//02-용도설명서
							else if("02" == fileGroup.get("att_cls_typ")){
								fileGroup.put("ag_att_no", fileGroup.get("grp_cd"));
							}
							//03-수입품목허가증
							else if("03" == fileGroup.get("att_cls_typ")){
								fileGroup.put("lcs_att_no", fileGroup.get("grp_cd"));
							}
							//04-소독제사용방법분류표
							else if("04" == fileGroup.get("att_cls_typ")){
								fileGroup.put("use_att_no", fileGroup.get("grp_cd"));
							}
							//99-기타
							else if("99" == fileGroup.get("att_cls_typ")){
								fileGroup.put("att_no", fileGroup.get("grp_cd"));
							}
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.casea.gn", fileGroup);
						}
						//B:구매요청HD
						else if("B" == fileGroup.get("job_typ")){
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.caseb.gn", fileGroup);
						}
						//C:단가계약요청HD
						else if("C" == fileGroup.get("job_typ")){
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.casec.gn", fileGroup);
						}
						//D:구매요청DT
						else if("D" == fileGroup.get("job_typ")){
							fileGroup.put("job_attr_1", fileGroup.get("job_attr_1")+"A");  // pr_no = job_attr_1 + 'A'
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.cased.gn", fileGroup);
						}
						//E:단가계약요청DT
						else if("E"==fileGroup.get("job_typ")){
							//단가계약번호 플래그 추가 2018.05.17
							fileGroup.put("job_attr_1", fileGroup.get("job_attr_1")+"B");  // pr_no = job_attr_1 + 'B'
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.casee.gn", fileGroup);
						}
						//F:신규물품심의
						else if("F"==fileGroup.get("job_typ")){
							//05-F:견적서
							if("05"==fileGroup.get("att_cls_typ")){
								fileGroup.put("req_att_no", fileGroup.get("grp_cd"));
							}
							//09-F:카탈로그
							if("09"==fileGroup.get("att_cls_typ")){
								fileGroup.put("cat_att_no", fileGroup.get("grp_cd"));
							}
							//03-수입품목허가증
							else if("03"==fileGroup.get("att_cls_typ")){
								fileGroup.put("lcs_att_no", fileGroup.get("grp_cd"));
							}
							//11-소모품정보
							else if("11"==fileGroup.get("att_cls_typ")){
								fileGroup.put("sub_item_att_no", fileGroup.get("grp_cd"));
							}
							//01-이미지
							else if("01"==fileGroup.get("att_cls_typ")){
								fileGroup.put("img_att_no", fileGroup.get("grp_cd"));
							}
							//13-용도설명서
							else if("13"==fileGroup.get("att_cls_typ")){
								fileGroup.put("uses_att_no", fileGroup.get("grp_cd"));
							}
							//14-식약처허가증
							else if("14"==fileGroup.get("att_cls_typ")){
								fileGroup.put("mfds_att_no", fileGroup.get("grp_cd"));
							}
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.casef.gn", fileGroup);
						}
						//G:불량진료재료
						else if("G"==fileGroup.get("job_typ")){
							// ESMMTBD에 update 로직 추가
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.mmtbd.gn", fileGroup);
						}

						//H:계약요청(공사)
						else if("H"==fileGroup.get("job_typ")){
							fileGroup.put("job_attr_1", fileGroup.get("job_attr_1")+"C");  // pr_no = job_attr_1 + 'C'
							$jdbc.update("common/interface_gn", "renewal.update.legacy.system.prdtc.gn", fileGroup);
						}
					}


					// 강릉 게이트웨이 FTP에서 파일 다운로드 시작
					var fileOkList = $jdbc.queryList( "common/interface_gn" , "renewal.select.esaatth.info.list.g", null);

					for(var i=0; i<fileOkList.size(); i++){
						var fileInfo = fileOkList[i];

						$jdbc.update("common/interface_gn", "update.gn.file.info.esaatth.sts.y", fileInfo);
					}



				}


				$logger.info("::::::::::batch.gn.file.info.download end");
			]]>
		</script>
	</service>






	<service id="get.po.test.test.test" label="발주남아있는것처리하자">
		<script>
			<![CDATA[
				$logger.info("::::::::::batch.gn.self.ord.y.send.iv start");
				var list = $jdbc.queryList("admin/template", "select.gn.test.test", null);


				for( var i = 0 ; i < list.size() ;  i++ ){
					var obj = list[i];

					$jdbc.update("procurement/po_vd", "update.podt.recv.auto", obj);

					/* 발주상태 인터페이스 : IF4093 */
					// 납품번호 생성
					var inv_no = $helper.generateCode('IV');
					obj.put("inv_no", inv_no);

					// 납품상세 Insert
					$jdbc.update("procurement/po", "insert.espivdt.auto", obj);

					// 납품헤더 Insert
					$jdbc.update("procurement/po", "insert.espivhd.auto", obj);

					// 발주상세 Update
					$jdbc.update("procurement/po", "update.esppodt.auto", obj);



				}


				$logger.info("::::::::::batch.gn.self.ord.y.send.iv end");
			]]>
		</script>
	</service>




	<!-- 단가계약서 대상조회 -->
	<service id="cha.test.test.test.test" label="단가 계약 대상조회">
		<script>
			<![CDATA[
				try
            	{
            		importPackage(Packages.emro.edoc.contract);
					importPackage(Packages.emro.std.approval);
					importClass(Packages.emro.util.StringUtil);

					var result = new Map();
		   			result.code = "S";

		            var strUtil     = new StringUtil();

            		var param = new Map();

            		param.put("cntr_no", "CN2021120282");
            		param.put("cntr_rev", "1");

            		// Interface 처리 ****************************************************
					importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);   // AFIS3.0 I/F
					var sendService    = new InterfaceSendProvider();

					importClass(Packages.volcano.custom.interfacemodule.InterfaceSendService);    // AFIS2.0 I/F
					var sendServAfs = new InterfaceSendService();

					// 병원별로 I/F가 필요함.
					var comp_cd_list = $jdbc.queryList("common/interface", "renewal.select.cntr.comp.list", param);
					var nSysIfList = $jdbc.queryList("common/interface", "renewal.select.list.n_sys_if_yn", null);


					for(var c=0; comp_cd_list.size()>c; c++) {
						var interfaceMap   = new Map();  //IF1183, IF1052:단가정보
								var interfaceMap2  = new Map();  //IF1145:계약체결정보
								var interfaceMap3  = new Map();  //IF1156:하자이행정보
								var interfaceMap4  = new Map();  //IF1144:입찰결과
								var interfaceMap5  = new Map();  //IF1122:첨부파일

						if(comp_cd_list[c].comp_cd == "70" || comp_cd_list[c].comp_cd == "30" || comp_cd_list[c].comp_cd == "40" || comp_cd_list[c].comp_cd == "60" || comp_cd_list[c].comp_cd == "20") {

								var n_sys_if_yn = "";
							    for(var j=0; nSysIfList.size()>j; j++) {
							    	if(nSysIfList[j].comp_cd == comp_cd_list[c].comp_cd) {
							    		n_sys_if_yn = nSysIfList[j].n_sys_if_yn;
							    		break;
							    	}
							    }

							    if (n_sys_if_yn == "Y") {

							    	// 복지물품은 인터페이스 대상 아님    wel_item_yn
							    	// 의약품은 인터페이스 대상 아님       drug_cnt
							    	// 불용자산매각 인터페이스 대상 아님 dis_cnt
							    	var cntr_if_chk = $jdbc.query("common/interface","renewal.select.cntr.if.chk",comp_cd_list[c]);
							    	if (cntr_if_chk.targ_cnt > 0 && cntr_if_chk.wel_item_yn == "N" && cntr_if_chk.drug_cnt == "0" && cntr_if_chk.dis_cnt == "0") {
								    	interfaceMap.put("if_typ",   "AFIS3HOS");
								    	interfaceMap.put("if_cd",    "IF1053");
								    	interfaceMap.put("comp_cd",  comp_cd_list[c].comp_cd);
								    	interfaceMap.put("cntr_no",  comp_cd_list[c].cntr_no);
								    	interfaceMap.put("cntr_rev", comp_cd_list[c].cntr_rev);
								    	//sendService.sendInterface(interfaceMap);    // AFIS3.0
								    }

							    }
							}
						}


            	}
            	catch(e)
	            {
	                $jdbc.rollback();
	                var err_msg  = "";
	                var err      = e.toString();
	                var ora_err  = err.substring(err.indexOf("ORA"), err.length);

	                $logger.info("????????????????????????");

	                if ( strUtil.isEmpty(ora_err) )
	                   err_msg = err;
	                else
	                   err_msg = ora_err;

	                result.code = "E";
	                result.msg = "오류가 발생을 하였습니다. " + "\n" + "에러내용" + " : " + err_msg;
	            }
			]]>
		</script>
	</service>

	<service id="item.test.test" label="아이템테스트">
		<script>
			<![CDATA[

           		importPackage(Packages.volcano.custom.interfacemodule);

				var a = new InterfaceGnCallback();

				a.testFunction_item();

			]]>
		</script>
	</service>

    <service id="send.batch.wbsarea.espivdt.01" label="2025-03-26 이창민 - 원내배송요청 전송 배치 추가">
        <script>
            <![CDATA[
                $logger.info("=== [START] send.batch.wbsarea.espivdt.01 ===");
                var retMap = new Map();
                // 원내배송요청 배치 대상 조회
                var list = $jdbc.queryList("common/interface", "renewal.auto.select.espivdt.if_bssend_yn", null);

                $logger.info("[START] renewal.auto.select.espivdt.if_bssend_yn :: " + list.size());

                importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
                var sendService = new InterfaceSendProvider();

                var iLoopCnt = 1;

                for(var i=0; i < list.size(); i++){
                    // 원내배송요청 Interface 처리 ********************************************************
                    var interfaceMap  = new Map();
                    interfaceMap.put("if_cd",   "IF5003");
                    interfaceMap.put("inv_no", list.get(i).inv_no);

                    retMap = sendService.sendInterface(interfaceMap);

                    $logger.info("[RESULT] log_sts :: " + list.get(i).inv_no + " - " + retMap.get("log_sts"));

                    if(retMap.get("log_sts") == "SUCCESS") {

                        $jdbc.update("common/interface", "renewal.auto.update.espivdt.if_bssend_yn_OK", interfaceMap);
                    } else {

                        $jdbc.update("common/interface", "renewal.auto.update.espivdt.if_bssend_yn_FAIL", interfaceMap);
                    }
                    $jdbc.commit();

                    iLoopCnt++;
                    $logger.info("[+++LCM+++] iLoopCnt: " + iLoopCnt);
                    if(iLoopCnt >= 150){
                        break;
                    }
                }


                // 2025-06-23 이창민 - READ TIME OUT 에러로 대용량 데이터는 1건씩만 전송하도록 추가
	            var listM = $jdbc.queryList("common/interface", "renewal.auto.select.espivdt.if_bssend_yn_MassData", null);
                $logger.info("[START] renewal.auto.select.espivdt.if_bssend_yn_MassData :: " + listM.size());

                if(listM.size() > 0){
                    interfaceMap.put("if_cd",   "IF5003");
                    interfaceMap.put("inv_no", listM.get(0).inv_no);
                    retMap = sendService.sendInterface(interfaceMap);

                    if(retMap.get("log_sts") == "SUCCESS") {

                        $jdbc.update("common/interface", "renewal.auto.update.espivdt.if_bssend_yn_OK", interfaceMap);
                    } else {

                        $jdbc.update("common/interface", "renewal.auto.update.espivdt.if_bssend_yn_FAIL", interfaceMap);
                    }
                    $jdbc.commit();
                }

                $logger.info("=== [END] send.batch.wbsarea.espivdt.01 === ");

            ]]>
        </script>
    </service>

    <service id="send.batch.baecha.esbscar.01" label="2025-03-26 이창민 - 배송차량 출발정보 전송 배치 추가">
        <script>
            <![CDATA[
                var retMap = new Map();
                importClass(Packages.volcano.custom.interfacemodule.InterfaceSendProvider);
                var sendService = new InterfaceSendProvider();

                var interfaceMap  = new Map();
                interfaceMap.put("if_cd",   "IF5004");

                retMap = sendService.sendInterface(interfaceMap);
            ]]>
        </script>
    </service>

	<service id="batch.renewal.mgp.auto.send.btlz_02" label="시약출고정보 송신(강릉AMS)">
		<script>
			<![CDATA[
				$logger.debug("=================[시약출고정보 송신(강릉AMS BATCH_START]=================");
				var paramMap = new Map();
				// EAI-시약불출정보를 조회
				var eaiDataList = $jdbc.queryList("common/interface", "renewal.mgp.auto.send.btlz_02", paramMap);
				$logger.debug("eaiDataList**********************************"+eaiDataList.size());

				// 송신된 시약 정보에 전송결과 업데이트
				for(var i=0; i < eaiDataList.size(); i++){
					var updateMap  = new Map();
					updateMap.put("eai_ties_manage_id", eaiDataList.get(i).eai_ties_manage_id);
					$logger.debug("eai_ties_manage_id**********************************"+eaiDataList.get(i).eai_ties_manage_id);
					$jdbc.update("common/interface", "renewal.mgp.auto.send.btlz_02.update", updateMap);
				}
			]]>
		</script>
	</service>

	<service id="batch.renewal.mgp.auto.send.btlz_03" label="시약출고정보 송신(AFIS)">
		<script>
			<![CDATA[
				$logger.debug("=================[시약출고정보 송신(AFIS)]=================");
				var paramMap = new Map();
				// EAI-시약불출정보를 조회
				var eaiDataList = $jdbc.queryList("common/interface", "renewal.mgp.auto.send.btlz_03", paramMap);
				$logger.debug("eaiDataList**********************************"+eaiDataList.size());

				// 송신된 시약 정보에 전송결과 업데이트
				for(var i=0; i < eaiDataList.size(); i++){
					var updateMap  = new Map();
					updateMap.put("comp_cd", eaiDataList.get(i).comp_cd);
					updateMap.put("eai_ties_manage_id", eaiDataList.get(i).eai_ties_manage_id);
					$logger.debug("comp_cd**********************************"+eaiDataList.get(i).comp_cd);
					$logger.debug("eai_ties_manage_id**********************************"+eaiDataList.get(i).eai_ties_manage_id);
					$jdbc.update("common/interface", "renewal.mgp.auto.send.btlz_03.update", updateMap);
				}
			]]>
		</script>
	</service>

	<service id="batch.renewal.mgp.auto.select.esppodt.recv_yn_02" label="MGP 발주접수정보  전송  전송(APROS->AMIS 강릉)">
		<script>
			<![CDATA[
				$logger.debug("=================[MGP 발주접수정보  전송  전송(APROS->AMIS 강릉)]=================");
				var paramMap = new Map();
				// MGP 발주접수정보를 조회
				var resultDataList = $jdbc.queryList("common/interface", "renewal.mgp.auto.select.esppodt.recv_yn_02", paramMap);
			]]>
		</script>
	</service>

	<service id="batch.renewal.mgp.auto.select.esppodt.recv_yn_03" label="MGP 발주접수정보  전송(AFIS)">
		<script>
			<![CDATA[
				$logger.debug("=================[MGP 발주접수정보  전송(AFIS)]=================");
				var paramMap = new Map();
				// MGP 발주접수정보를 조회
				var resultDataList = $jdbc.queryList("common/interface", "renewal.mgp.auto.select.esppodt.recv_yn_03", paramMap);
			]]>
		</script>
	</service>

	<service id="batch.renewal.mgp.auto.select.espivhd_02" label="MGP 납품등록 전송(강릉AMS)">
		<script>
			<![CDATA[
				$logger.debug("=================[MGP 납품등록 전송(강릉AMS)]=================");
				var paramMap = new Map();
				// MGP 납품등록를 조회
				var resultDataList = $jdbc.queryList("common/interface", "renewal.mgp.auto.select.espivhd_02", paramMap);
			]]>
		</script>
	</service>

	<service id="batch.renewal.3pl.auto.select.espivhd_03" label="MGP 납품등록 전송(AFIS)">
		<script>
			<![CDATA[
				$logger.debug("=================[MGP 납품등록 전송(AFIS)]=================");
				var paramMap = new Map();
				// MGP 납품등록를 조회
				var resultDataList = $jdbc.queryList("common/interface", "renewal.3pl.auto.select.espivhd_03", paramMap);
			]]>
		</script>
	</service>

	<service id="batch.renewal.mgp.auto.select.espivhd.cancel_02" label="MGP 납품취소 전송(강릉AMS)">
		<script>
			<![CDATA[
				$logger.debug("=================[MGP 납품취소 전송(강릉AMS)]=================");
				var paramMap = new Map();
				// MGP 납품취소를 조회
				var resultDataList = $jdbc.queryList("common/interface", "renewal.mgp.auto.select.espivhd.cancel_02", paramMap);
			]]>
		</script>
	</service>

	<service id="batch.renewal.mgp.auto.select.espivhd.cancel_03" label="MGP 납품취소 전송(AFIS)">
		<script>
			<![CDATA[
				$logger.debug("=================[MGP 납품취소 전송(AFIS)]=================");
				var paramMap = new Map();
				// MGP 납품취소를 조회
				var resultDataList = $jdbc.queryList("common/interface", "renewal.mgp.auto.select.espivhd.cancel_03", paramMap);
			]]>
		</script>
	</service>

	<service id="batch.renewal.mgp.auto.select.espivsn_02" label="MGP 납품일련번호정보(강릉AMS)">
		<script>
			<![CDATA[
				$logger.debug("=================[MGP 납품일련번호정보(강릉AMS)]=================");
				var paramMap = new Map();
				// MGP 납품일련번호정보를 조회
				paramMap.put("inv_no", "I20211100113")
				var resultDataList = $jdbc.queryList("common/interface", "renewal.mgp.auto.select.espivsn_02", paramMap);
				$logger.debug("resultDataList**********************************"+resultDataList.size());

			]]>
		</script>
	</service>

	<service id="batch.renewal.mgp.auto.select.espivsn_03" label="MGP 납품일련번호정보(AFIS)">
		<script>
			<![CDATA[
				$logger.debug("=================[MGP 납품일련번호정보(AFIS)]=================");
				var paramMap = new Map();
				paramMap.put("inv_no", "I20211100264")
				// MGP 납품일련번호정보를 조회
				var resultDataList = $jdbc.queryList("common/interface", "renewal.mgp.auto.select.espivsn_03", paramMap);
			]]>
		</script>
	</service>

</service-descriptor>
